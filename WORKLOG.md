# Bennett.jl Work Log

> # 🚨 NEXT AGENT — START HERE 🚨
>
> **🎯 NEXT AGENT — 2026-05-22 (gf3n EPIC COMPLETE — M1+M2+M3+M4 all CLOSED).** `Bennett-gf3n` Julia heap-memory support is **done**. Under `mem=:heap`: M1 (`Bennett-gps7`) drops a heap collection dead w.r.t. the return; M2 (`Bennett-kuza`) re-roots `Array{T}(undef,const_N)`; M3 (`Bennett-5ikt`) compiles a `push!`-built `Vector` with a statically-inferable count (recognises the `@j_#_growend!` capacity diamonds as dead skeleton, infers N from the element-store offsets, re-roots onto a synthetic `IRAlloca`); M4 (`Bennett-bd5f`) is fail-loud scope hardening — `Dict` / mid-array `insert!`/`deleteat!` / C-Rust `malloc` / runtime-loop `push!` / runtime-N `Array` all reject with a precise signpost message. **Corpus fixture TJ1 (`push!×3 + reduce`) is genuinely green** (326 gates, oracle `3x+3`). All in `src/extract/heap.jl` — no lowering change, no new IR node; M1/M2/M3 via the 3+1 protocol, M4 standard. See [`worklog/074`](worklog/074_2026-05-22_5ikt_heap_m3.md) (M3+M4) and [`worklog/073`](worklog/073_2026-05-21_gps7_heap_m1.md) (M1/M2). **NEXT — the dynamic-memory frontier past gf3n:** `Bennett-800b` (Julia `Dict` — a separate research program; front-end recognition is the blocker; M4 now rejects Dict loud with a `Bennett-800b` pointer) and `Bennett-890r` (rewrite the TJ4 corpus fixture — today a store-to-load mirage — with distinct store/load indices). Neither was on the gf3n critical path. `Bennett-25dm` (acceptance umbrella) closes only when gf3n AND 800b land, so it stays open. Also open from the M-series: `Bennett-5006` (runtime-index element stores), `Bennett-chz7` (P4 — `shard_worklog.py` hardcoded-path bug). The text below is the pre-M1 handoff, retained for context.
>
> **🎯 NEXT AGENT — AIM: LAND DYNAMIC MEMORY (2026-05-21 handoff).** The strategic direction for upcoming work is the **dynamic-memory frontier** — making genuine heap-allocated memory compile. It is the one coherent program left with real user-facing value (the "toy → real" step); everything else in the backlog (~47 active items) is an optional long tail. **START: `Bennett-gf3n` Milestone M1** — GC-preamble recognition + the dead-skeleton liveness proof + the `mem=:heap` opt-in mode. Read `docs/design/heap_memory_consensus_design_2026-05-21.md` (consensus design) and `..._brief_2026-05-21.md` (problem statement) FIRST. M1 needs its own 3+1 (core extraction change; the taint/liveness proof is the subtle, highest-risk part). The validation spike that gates this work is **already done and passed** — M1 may be scheduled immediately. **FIRST, a cleanup — retire `Bennett-cc0.5`:** it is stale and superseded — its premise ("handle thread_ptr GEP base") was disproven by the 2026-05-21 spike (the wall is an inline-asm TLS read, not a GEP) and `Bennett-gf3n` re-scopes the whole problem. Run `bd close Bennett-cc0.5 --reason="superseded by Bennett-gf3n; thread_ptr-GEP premise disproven by the 2026-05-21 heap de-risking spike — see worklog/072"`. Note `Bennett-25dm` is an acceptance umbrella, not pickup work (it closes when gf3n + 800b land). **Program map:** `gf3n` (P2, Vector/Array, milestones M1-M4) is the spine; `Bennett-800b` (P3, Dict — separate research program, front-end recognition is the blocker); `Bennett-glh` (P2, Julia EscapeAnalysis); C/Rust ingest (`n88f`/`i3nj`/`xkv`); `Bennett-890r` (TJ4 mirage test fix). (Historical: the T5 persistent-DS epic is complete — all four `persistent_impl` arms wired, `:linear_scan` the confirmed default.) **Chunk 072** ([`worklog/072_2026-05-21_qrom_paper_assessment.md`](worklog/072_2026-05-21_qrom_paper_assessment.md)) — 2026-05-21, three sessions. **(A) Heap-memory 3+1 design effort** (design-only) — validated extraction-only design for Julia `Vector`/`Array` support: recognise+strip the dead GC skeleton, re-root surviving stores/loads onto a synthetic const-capacity `IRAlloca`, compile with existing `:shadow` lowering (no new IR node, no lowering change). Dead-skeleton hypothesis confirmed by a validation spike. Design doc `docs/design/heap_memory_consensus_design_2026-05-21.md`; 4-milestone roadmap filed as epic **Bennett-gf3n** (M1-M3 each need their own 3+1; TJ1 goes green at M3; `Dict` out of scope). **(B) Bennett-8su4 CLOSED** — volatile `c=0` GC-frame memset now accepted (predicate relocation in `_handle_memset_arm`, full 3+1). The heap-memory de-risking spike found that compiling Julia `Vector`/`Dict`/`Array(undef)` to green corpus fixtures is a multi-effort research program — wall map: inline-asm TLS read (`movq %fs:0`, Bennett-5oyt) → `@ijl_gc_small_alloc` heap-pointer tracking → irreversible runtime callees (`growend!`/`setindex!`). NOT a single ~500-LOC `cc0.5` fix; `cc0.5`/`25dm` bead descriptions are stale and need re-triage. SROA-dissolution fear REFUTED. `Bennett-890r` filed (TJ4 is a store-to-load mirage). **(C) QROM optimization paper assessment:** Motlagh-Pocrnic 2026 "Halving the cost of QROM" (arXiv:2605.20334) found NOT applicable (dirty-ancilla halving; Bennett's ancillae are clean); Low-Kliuchnikov-Schaeffer 2024 (arXiv:1812.00954) SelectSwap/QROAM identified as the right clean-ancilla reference but DEFERRED — QROAM only beats unary QROM at `L>4W` and every table Bennett emits today is below that crossover. Research bead `Bennett-p4ch` filed; no code changed. **(D) Backlog + opcode stocktake + this handoff.** Full 2026-05-21 backlog stocktake is in chunk 072: ~47 active items = the dynamic-memory program (the handoff directive above) + a long tail of optional circuit-cost optimizations, memcpy edge-cases, deferred research, and publishing/hygiene. **No P0/P1 items, no critical bugs** — the core compiler is ~89% closed (464/522 beads). Work should be aimed squarely at the dynamic-memory program; the long tail is pick-up-when-convenient.
>
> **2026-05-20 orchestration-session summary (4 beads shipped serial: d746 + qi6c + ktt8 + 2uas; all pushed to `origin/main`):**
>
> 1. `d746` (1eef0a7, PUSHED) — wire `:hamt` persistent_impl arm (byte-template of Bennett-6883/`:okasaki`). `verify_pmap_correctness(HAMT_IMPL)=true`. 6 peer-regression files green. Filed `Bennett-2xws` (HAMT mis-stores keys colliding mod 32 — single-level node, no collision node).
> 2. `qi6c` (5bd80ae, PUSHED) — wire `:cf` persistent_impl arm — **all four impls now wired**. `verify_pmap_correctness(CF_IMPL)=true` (the Bennett-n3z4/U21 reroot-key=0 regression has not reappeared). `_resolve_persistent_impl` unconditional NYI `else` branch retired. testset 6 consolidated (impl-NYI rotation ended; hashcons-NYI + bogus-symbol probes). CF 3-key demo total=2880 — ~9× cheaper than okasaki/hamt.
> 3. `ktt8` (6acc7f8, PUSHED) — T5-P7a head-to-head Pareto benchmark, `benchmark/bc_t5_head_to_head.jl` (520 LOC). Full 64-cell sweep. `:linear_scan` wins every cell (~1150–1450 gates, flat in depth); cf 2nd; okasaki worst. Bead text (2026-04-17) was doubly stale (3 hashcons states + wide W — neither wired); scope-corrected. Filed `Bennett-7sb7` (HAMT won't compile `optimize=false`) + `Bennett-8o70` (wide-W callees NYI).
> 4. `2uas` (1cb8b06, PUSHED) — T5-P7b docs pass closing the epic: BENCHMARKS.md Pareto subsection, `docs/paper_outline_T5.md` (NEW — §T5 paper section for Bennett-6siy), README feature table. Docs only.
>
> **Net result:** T5 epic closed. 4 beads closed, 3 follow-up beads filed (7sb7, 2xws, 8o70 — all P3, all open). Orchestration lesson: benchmark beads filed far ahead of implementation accrete stale axes — scope-check against what's actually wired before delegating (ktt8's W and hashcons axes were both dead).
>
> ---
>
> **2026-05-16 orchestration-session summary (4 beads shipped serial: jpa5 + doih + zxhg + land; all pushed to `origin/main`):**
>
> -1. `land` (0413890, PUSHED) — ptr-typed ConstantStruct field materialisation (zxhg follow-up; first t5 corpus Rust HashMap::new shape end-to-end). `_flatten_struct_to_bytes` PointerType arm split out: `_ptr_identity` arm `(:named, ref)` → assign synthetic 64-bit LE address via per-module monotonic counter (`0x1000_0000_0000_0000 | n`); `(:null, 0)` → 8 zero bytes; `(:addr, K)` and `nothing` still reject (filed `Bennett-6qsn` for inttoptr follow-up). Non-zero addrspace + ptr-size != 8 hard-reject (Bennett-lj10, rpqc). New `ParsedIR.synth_ptr_provenance::Set{Tuple{Symbol,Int,Int}}` field surfaces ptr-field byte ranges to a downstream load-escape guard at `_handle_load` that fails loud (`Bennett-land-ptrload`) when synth-tagged-alloca bytes are consumed by anything other than another `llvm.memcpy.*`. Carry-through propagation in standard memcpy arm preserves the tag through chained memcpys (HashMap::new pattern: `@anon → %_3 → %_2 → %_0`). All new threading via kwargs (q04a 9/9 preserved). 3+1; new test file 65/65 (extraction-only positives + carry-through end-to-end + 4 reject classes + determinism + T5 acceptance). zxhg 3 testsets flipped (was reject, now positive with "MUST NOT be zxhg-ptrfield" residual contract). 9-peer regression all green (doih 85/85, 8kno 8/8, 37mt 86/86, 9nwt 87/87, ixiz 53/53, munq 69/69, lqif 12/12, q04a 9/9, gate-count 39/39, zxhg flipped 83/83). 5 follow-up beads filed (6qsn inttoptr, lj10 addrspace, rpqc ptrsize32, 4rl0 precise-escape, fnuf transitive-escape). **Caveat:** actual `build/t5_tr2_hashmap.ll` HashMap::new compile blocked by UPSTREAM LLVM-version skew (`trunc nuw` is LLVM 19+ syntax against LLVM 18 toolchain) — NOT a Bennett bug; structural mirror fixture `land_t5_tr2_acceptance.ll` proves the bead's actual shape works.
> 0. `zxhg` (3cbe8c1, PUSHED) — ConstantStruct global extraction (doih follow-up). `_extract_const_globals` extended with 3 dispatch arms (ConstantDataArray unchanged + new ConstantStruct + ConstantAggregateZero-of-Struct/Array). New helper `_flatten_struct_to_bytes` uses `LLVM.offsetof`/`abi_size` so packed AND non-packed structs handled in one path. Pure-integer structs (incl. nested + ABI-padded) extract as byte-granular `(UInt64.(bytes), 8)`; any non-integer field (ptr/float/vector/i128) hard-rejects → silently skipped → G5 fires `Bennett-zxhg-ptrfield` breadcrumb (this is what t5_tr2_hashmap.ll:153 hits). G5 message at instructions.jl:483-498 rewritten. 3+1 protocol; new test file 90/90 + doih flipped testset (77→85) + 8-peer regression all green. doih fixture renamed `..._struct_reject.ll` → `..._struct.ll`. 1 follow-up bead filed (`Bennett-land` P4 — closed same day, see -1 above).
> 0a. `doih` (1c9d96f, PUSHED) — global-pointer src memcpy under Bennett-hao Phase 3 / Bennett-8bys umbrella. Predicate 5 in `_handle_memcpy_arm` split into 5a (DST-as-global hard reject) and 5b (SRC-as-global → new `_handle_memcpy_global_src` arm). `parsed.globals` dict threaded through `_module_to_parsed_ir_on_func` → `_convert_instruction` (NEW kwarg with empty-dict default; q04a positional contract preserved) → `_handle_intrinsic` → `_handle_memcpy_arm` → new arm. G1-G9 cascade emits K element-granular `IRPtrOffset + IRStore(iconst, dst_ew)` chunks (mirror of Bennett-9nwt case C, per-element from globals dict). 77/77 in new `test_doih_memcpy_global_src.jl`, 8 peer regressions all green, zero pre-existing tests flipped. **Bead acceptance fixture (t5_tr2_hashmap.ll:153) unreachable by MVP** — both proposers caught that the referenced global is `ConstantStruct<{ ptr, [24 x i8] }>`, not ConstantDataArray; `_extract_const_globals` silently skips it. Honest scope tightening per the Bennett-ixiz lesson; filed `Bennett-zxhg` follow-up (closed same day). 3 follow-up beads total filed (`Bennett-zxhg` P3 struct, `Bennett-epfe` P4 cross-width, `Bennett-ui4f` P4 var-GEP true QROM).
> 0b. `jpa5` (a83842f, PUSHED) — triaged + closed as won't-fix. The bead's REWORK annotation was correct (the `if length(full) == W` predicate at `src/lowering/arith.jl:230` is structurally unreachable today because LLVM mul always has matching operand/result widths and `_iwidth(inst)` reads result type). Option (c) "detect zext-provenance + fire producer-tag" ALSO rejected because the annotation missed a second blocker: `_infer_self_reversing` (bennett_transform.jl:176, condition 4) rejects any LR whose non-tagged groups aren't `__pred_*`/`__ret_*`/`__branch_*` — a zext-fed widening mul has IRCast groups holding real data. Real fix needs core 3+1 covered better by sibling `Bennett-pzft`. Drive-by: comment block at arith.jl:218-247 rewritten to name the structural blockers honestly. No code-behavior change. Targeted regressions green; full Pkg.test skipped per user preference.
> 1. `hzl9` (96b05cb) — producer-tag for `lower_tabulate` (h0ai follow-up A). Emits one `:__tabulate_qrom` GateGroup over the entire QROM block + threads `auto_self_reversing` kwarg into both `reversible_compile` tabulate call sites (Bennett.jl:296,307) — kill switch was silently ignored on the tabulate path pre-hzl9, now honored end-to-end on `strategy=:tabulate` AND `:auto`-picks-tabulate. 3+1 protocol; +261 asserts.
> 2. `rjk7` (6a8c50c) — universal `lr.self_reversing` fast-path (h0ai follow-up F). All 6 strategies now honor the short-circuit via inlined 4-line `_validate_self_reversing!` + `_build_circuit` pattern. **Genuine contract gap found**: pre-rjk7 `EagerStrategy` silently accepted forged tags; the other 4 strategies threw via inconsistent algorithm-internal checks, not U03. Post-rjk7 all 6 throw `ArgumentError` uniformly per §1. 3+1; new test file with 18 testsets / 3098 asserts.
> 3. `ixiz` (THIS COMMIT) — wider-element alloca support (hao Phase 3 sub-bead). 5 gate sites lifted (G1 in aggregate.jl, G3+G4+G5 in extract/instructions.jl, plus a 4th implementer-caught duplicate at _convert_instruction:2032). **Bead description was wrong**: said "two gate sites including memory.jl:245-246" but Gate 2 already checks `inst.width == elem_w` (not `== 8`) and serves as the mixed-width firewall. Real scope was 5 sites in extract+lowering. Both proposers independently caught the framing error; implementer caught a 5th site. 3+1; new test file 53/53 + 4 reject-test flips (test_munq, test_37mt, test_9nwt, test_lqif — last was implementer's drive-by). 2 sibling beads filed (2fue P3 mixed-width, a5ag P4 (N=1,W>=64) gap).
> 4. `2ebq` — closed as superseded by `lx5h` (byte-identical descriptions; lx5h shipped the 8 float vector-reduction dispatch arms 2026-05-15 at src/extract/vectors.jl:246-253). No-op close.
> 5. `jpa5` — annotated with rework finding (NOT closed). The bead's premise ("raw IR ingest produces mul with dest=2W via 2-bit-width promotion") is not a pattern the current extractor can express — IRBinOp has a single width field; extractor at src/extract/instructions.jl:1407-1413 reads LLVM result type. Three reframe options filed; bead needs triage before next pickup.
>
> **Net result:** h0ai cluster narrowing. Out of 5 follow-ups filed 2026-05-15, two closed (hzl9 yesterday, rjk7 today); three remain (jpa5 needs reframe, pzft P4, lxk7 P4). Open-bead count 35 → 33 (rjk7 + 2ebq).
>
> **Orchestration lessons worth keeping (two from today's three closes):**
>
> **Lesson 1 (rjk7):** the RED-state evidence (T13-T18 forged-tag tests run pre-Step-2 via stashed edits) showed that of the 5 non-default strategies, only `EagerStrategy` actually silently-accepted the forged tag; the other 4 threw, but each via a different algorithm-internal check (PebbledGroup wire bookkeeping, Checkpoint structural rejects, etc.) — never via the U03 contract probe. The "fail loud" property was non-uniform across the strategy matrix pre-rjk7, even though it looked uniform from a "does it throw?" perspective. Take-home: when extending a contract across an axis (strategies, dispatch arms, etc.), measure WHERE the throw originates, not just WHETHER it throws.
>
> **Lesson 2 (ixiz):** the bead description cited "two gate sites" but BOTH proposers (working independently) verified the source and found the framing was wrong — memory.jl Gate-2 sites are already same-width-correct and serve as the mixed-width firewall, NOT a blocker. The real gates were FOUR sites in extract+lowering, and the implementer caught a FIFTH duplicate. Lifting only Gate 1 (per the bead's narrowest reading) would have shipped dead code. Take-home: bead descriptions are user-filed and can be wrong — orchestrators MUST verify them against source before scoping. The 3+1 protocol caught this because two independent proposers both read the source skeptically. Per CLAUDE.md §10: skepticism applies to bead text too.
>
> **Next agent decision point:** the h0ai cluster is winding down. Three natural directions for the next pickup:
> (a) `jpa5` triage — pick one of the three reframe options and decide whether the bead is worth keeping at all (the dead-code predicate at arith.jl:230 is structurally unreachable today regardless);
> (b) advance T5-P6 (`z2dj` IN-PROGRESS) — biggest critical-path item, blocks the `25dm` bug bead;
> (c) pick from the `hao` Phase 3 family (post-doih: 4 mechanical P3 beads remain — xtu9, yxr8, zmry, 6c6f — under the 8bys umbrella; ixiz + doih closed 2026-05-16).
>
> ---
>
> **Read [`worklog/070_2026-05-16_doih_global_src_memcpy.md`](worklog/070_2026-05-16_doih_global_src_memcpy.md) FIRST** — 2026-05-16 (top): **Bennett-doih close** — global-pointer src memcpy under Bennett-hao Phase 3 / Bennett-8bys umbrella. Predicate 5 in `_handle_memcpy_arm` split into 5a (DST-as-global hard reject) and 5b (SRC-as-global → new `_handle_memcpy_global_src` arm). `parsed.globals` dict threaded through `_module_to_parsed_ir_on_func` → `_convert_instruction` (NEW kwarg with empty-dict default; q04a positional contract preserved) → `_handle_intrinsic` → `_handle_memcpy_arm` → new arm. G1-G9 cascade emits K element-granular `IRPtrOffset + IRStore(iconst, dst_ew)` chunks (mirror of Bennett-9nwt case C, per-element from globals dict). 77/77 in new `test_doih_memcpy_global_src.jl`, 8 peer regressions all green, zero pre-existing tests flipped. **Bead acceptance fixture (t5_tr2_hashmap.ll:153) unreachable by MVP** — both proposers caught that the referenced global is `ConstantStruct<{ ptr, [24 x i8] }>`, not ConstantDataArray; `_extract_const_globals` silently skips it. Honest scope tightening per the Bennett-ixiz lesson; file `Bennett-zxhg` follow-up for ConstantStruct extension. 3 follow-up beads filed (`Bennett-zxhg` P3 struct, `Bennett-epfe` P4 cross-width, `Bennett-ui4f` P4 var-GEP true QROM).
>
> **Then read [`worklog/069_2026-05-16_rjk7_self_reversing_all_strategies.md`](worklog/069_2026-05-16_rjk7_self_reversing_all_strategies.md)** — 2026-05-16 (top): **Bennett-rjk7 close** — universal `lr.self_reversing` fast-path extended from `DefaultStrategy`-only to all 6 strategies. 5 strategy `_*_bennett_impl` bodies prepend the EXACT same 4-line check (mirrors `_bennett_default` at `bennett_transform.jl:286-294`). **Critical contract gap finding**: pre-rjk7 `EagerStrategy` silently accepted forged self_reversing=true tags; other 4 non-default strategies threw but via inconsistent algorithm-internal checks (not U03). Post-rjk7 all 6 throw `ArgumentError` uniformly via `_validate_self_reversing!` per §1. Pre-rjk7 PebbledStrategy(0) was a "free rider" — its `max_pebbles<=0` fallback `return bennett(lr)` re-entered DefaultStrategy via dispatch. 3+1 synthesis: inline (B's pick) over helper (A's pick); central docstring (B) over central+per-strategy (A); parametrized-loop-with-named-inner-testset (synthesis). Order: test file first → red on EagerStrategy/CheckpointStrategy/PebbledGroupStrategy/ValueEager → 5 strategy edits → docstring update. 3098/3098 asserts in new test_rjk7_self_reversing_all_strategies.jl (3 groups × 6 strategies = 18 inner testsets). Peer regressions all green (test_self_reversing 12/12, test_egu6 264/264, test_h0ai 132,428/132,428, test_gate_count_regression 39/39 unchanged). Side cleanups same session: Bennett-2ebq closed as superseded by lx5h; Bennett-jpa5 annotated with rework finding (premise not expressible by current extractor).
>
> **Then read [`worklog/068_2026-05-15_h0ai_auto_self_reversing.md`](worklog/068_2026-05-15_h0ai_auto_self_reversing.md)** — has BOTH the 2026-05-15 hzl9 close (top — producer-tag for lower_tabulate + auto_self_reversing kill-switch threading) AND the 2026-05-15 h0ai infrastructure close (below). hzl9 commit 96b05cb pushed 2026-05-15 evening; chunk 068 capture written during that session.
>
> ---
>
> **2026-05-15 orchestration-session summary (8 beads serial, all pushed to `origin/main`):**
>
> 1. `kh6n` (a0153de) — trailing-`.` prefix discipline for 32 scalar `llvm.*` sites + 3 fail-loud rejects.
> 2. `k2w6` (715c8aa) — `soft_fmin/fmax/fminimum/fmaximum`; closes kh6n's first stub.
> 3. `mq6f` (45e2b3d) — `soft_round_away` (round-half-AWAY); CORRECTS kh6n's misstatement about `soft_round` semantics; fixes a real silent-miscompile of `llvm.round.f64`.
> 4. `p19b` (3a02aec) — `soft_minimumnum/maximumnum` LLVM 19+ aliases; closes the LAST kh6n stub (zero `_ir_error` rejects from kh6n remain).
> 5. `pg5` (dd8612e) — 9 integer `llvm.vector.reduce.*` (add/mul/and/or/xor/smax/smin/umax/umin).
> 6. `lx5h` (b4c5768) — 8 float `llvm.vector.reduce.*` (fadd/fmul/fmin/fmax/fminimum/fmaximum/fminimumnum/fmaximumnum); leverages k2w6+p19b primitives.
> 7. `h0ai` (dbd5845) — auto self-reversing detection infrastructure via 3+1 protocol (Plan-A + Plan-B + Implementer). Infrastructure verified at the mechanism level but **no real-world function triggers it today** — see h0ai entry for why.
> 8. (`0q0o` filed and immediately closed-as-wrong-premise — superseded by `mq6f`.)
>
> **Net result:** every LLVM round / min/max / vector-reduction intrinsic dispatches natively. `_CALLEES_FP_ROUND` 4→5; new `_CALLEES_FP_MINMAX` group of 6; 17 vector reductions covered. ~1.4M assertions added across 7 new test files. Soft-float primitive count 32 → 39.
>
> **Orchestration lesson worth keeping:** the 3+1 protocol on h0ai caught one bead-premise flaw (Proposer A + B both flagged the entry-block predicate NOTGate; A additionally caught the truncation high-W stranding for `(x,y)->x*y, Int8, Int8`). The orchestrator (me) verified A's truncation point empirically and confidently asserted the WIDENING case `Int16(x)*Int16(y), Int8, Int8` would auto-promote — WITHOUT re-verifying. The implementer caught my error: `code_llvm` shows Julia lowers `Int16(x)*Int16(y)` to a single `mul i16` (W=16), so qcla_tree's 2W=32 wires get sliced to 16 just like the truncating case. **Take-home:** when a Plan proposer flags a bead-premise issue, the orchestrator's verification of the proposed fix needs an empirical probe too, not a confident assertion. One `code_llvm` call would have caught it.
>
> **5 h0ai follow-ups filed (path to making h0ai actually fire):**
> - `Bennett-hzl9` — extend producer-tag to `lower_tabulate` (QROM) via `lower(parsed::ParsedIR)`
> - `Bennett-jpa5` — non-slicing qcla_tree variant for raw IR ingest
> - `Bennett-pzft` (P4) — uncompute high-W qcla_tree wires for the truncating mul case
> - `Bennett-rjk7` — extend self-reversing fast-path to non-default Bennett strategies (Eager/ValueEager/etc.)
> - `Bennett-lxk7` (P4) — extend U03 probe to randomised-input battery
>
> **1 pg5 follow-up filed:**
> - `Bennett-lx5h` — float vector reductions (CLOSED same session as bead 6 above)
>
> **Next agent decision point:** the round/min/max/reduce surface is closed. Three natural directions:
> (a) close one of the 5 h0ai follow-ups (hzl9 is highest-leverage — would make auto-promotion fire on `lower_tabulate`-emitted circuits = QROM tables = real-world);
> (b) tackle Bennett-9wmk's design puzzle (OPEN with finding from chunk 065 — needs `parallel_adder_tree` pool recycling design + Plan agent);
> (c) pivot to docs (Bennett-7kzr — full-scale post-Enzyme-parity refresh, slow per `feedback_doc_work_mode.md`).
>
> ---
>
> **Read [`worklog/068_2026-05-15_h0ai_auto_self_reversing.md`](worklog/068_2026-05-15_h0ai_auto_self_reversing.md) FIRST** — 2026-05-15 (top): **Bennett-h0ai close** — auto self_reversing detection for `lower(parsed::ParsedIR)` via 3-layer protocol: producer-tag (`GateGroup.is_self_reversing::Bool`, set by qcla_tree dispatch's non-truncating branch via a `LoweringCtx.last_inst_self_reversing::Ref{Bool}` side-channel) + structural aggregator (`_infer_self_reversing(lr, predicate_wires)`: no branching AND exactly one tagged group AND `tagged.result_wires == lr.output_wires` AND every other group is `__pred_*`/`__ret_*`/`__branch_*` boilerplate) + runtime probe (`_validate_self_reversing!(lr; trusted_dirty_wires=…)` with the entry-block predicate wires exempted via the new kwarg allowlist; forged tags still caught because the allowlist is bounded). New `auto_self_reversing::Bool=true` kwarg on `lower()` + all 4 `reversible_compile` overloads (Tuple / ParsedIR / Float64 / CompileOptions) plus `CompileOptions.auto_self_reversing` field. **CRITICAL EMPIRICAL FINDING (forced design correction):** the orchestrator's spec claimed `(x,y)->Int16(x)*Int16(y), Int8, Int8; mul=:qcla_tree` would auto-promote because qcla_tree's full 2W output IS the function output — empirically FALSE under the current arith.jl:218 `[1:W]` slice. `code_llvm` shows Julia lowers `Int16(x)*Int16(y)` to a single `mul i16` (W=16); qcla_tree emits 2W=32 wires; the slice drops 16 just like the truncating Int8 case. Direct U03 probe verification: TRUNC fails, FULL passes. Consequence: under current arith.jl:218 dispatch, the producer-tag NEVER fires. h0ai is **conservative-only today** — infrastructure verified correct via mechanism-level T1 (hand-built LR with one tagged group); savings land when a follow-up bead opts in a producer (lower_tabulate via `lower()`, or non-slicing qcla_tree variant). 131,356/131,356 asserts in new `test_h0ai_auto_self_reversing.jl` (13 testsets: T1 mechanism-YES, T2-T11 NO cases incl. forged-tag-fails-loud + lower_tabulate-path-unchanged, T12 fold composition, T13 kwarg threading). Peer regressions all green: egu6 (264/264), self_reversing P1 (12/12), mul_dispatcher (196,697/196,697), 4fri (36/36), heup fold_constants (539/539), gate_count_regression (39/39), intrinsics (1280/1280), lx5h (71/71), pg5, increment, pebble strategies, softfexp, polynomial. 3+1 protocol observed. Five h0ai follow-ups filed (A producer-tag for lower_tabulate / D non-slicing qcla_tree / E uncompute high-W / F extend self-reversing fast-path to non-default strategies / G randomised U03 probes).
>
> **Then read [`worklog/067_2026-05-15_pg5_vector_reductions.md`](worklog/067_2026-05-15_pg5_vector_reductions.md)** — 2026-05-15 (top): **Bennett-lx5h close** — native dispatch for the 8 LLVM **float** vector-reduction intrinsics (`llvm.vector.reduce.{fadd,fmul,fmin,fmax,fminimum,fmaximum,fminimumnum,fmaximumnum}.*`), the float follow-up to pg5. Extends `_handle_vector_reduction` in `src/extract/vectors.jl` with 8 more dispatch arms folding lanes via `IRCall` over the matching `soft_*` primitive (all 8 already in callee registry per k2w6 + p19b). New `(:fcall, soft_fn)` dispatch kind alongside pg5's `(:binop, sym)` / `(:cmp, pred)`. Structural wrinkle: `fadd`/`fmul` carry a SCALAR START arg (`[start, vector, callee]` operand layout vs `[vector, callee]`); fold uses `start` as initial accumulator and folds ALL N lanes. Strict left-to-right per CLAUDE.md §13 — LLVM `reassoc` flag IGNORED for bit-exactness. 71/71 in new `test/test_lx5h_float_vector_reductions.jl` (10 fixtures: 8 intrinsics × v4f64 + v2f64 fadd corner + 2 f32 reject); pg5 testset updated to verify the dispatch boundary moved (56/56 still). Peer regressions all green: ao66, kh6n, k2w6, mq6f, p19b, intrinsics, float_intrinsics. 3+1 skipped per mechanical-extension exception (mirrors pg5's pattern with soft_* callees). Below it: **Bennett-pg5 close** — native dispatch for the 9 LLVM integer vector-reduction intrinsics. New `_handle_vector_reduction` helper in `src/extract/vectors.jl` runs inside `_convert_vector_instruction`'s `LLVMCall` arm BEFORE the existing "scalar return type" reject. Lowers a vector reduction to a left-to-right linear fold chain over already-extracted lanes (binop or `IRICmp + IRSelect` for min/max). Trailing-`.` discipline on every op token per Bennett-kh6n. 56/56 (now 54/54 post-lx5h) assertions; peer regressions all green.
>
> **Then read [`worklog/066_2026-05-15_kh6n_prefix_discipline.md`](worklog/066_2026-05-15_kh6n_prefix_discipline.md)** — 2026-05-15 (top): **Bennett-p19b close — THE kh6n FUTURE-WORK PIPELINE IS NOW CLOSED.** Native dispatch for IEEE 754-2019 `llvm.minimumnum.f64` / `llvm.maximumnum.f64` (LLVM 19+) via thin `function … end` aliases over the existing `soft_fmin` / `soft_fmax` (Bennett-k2w6 already chose the spec'd ±0 tie-break, so the bodies are bit-identical). LLVM dispatch arms in `src/extract/instructions.jl` replace the third (and last) kh6n explicit-reject with native `IRCall`. `_CALLEES_FP_MINMAX` extended +2 (now 6). Module export +2 (37 → 39 soft-float primitives). 227,276 / 227,276 asserts green incl. alias-identity invariant (5k random + 15 corner pairs), 100k random vs IEEE 754-2019 reference × 2 primitives, ±0 / NaN / ±Inf matrix, IR ingest + f32 reject, 1074-binade subnormal-input sweep × ± × 4 partner operands × 2 primitives. After p19b: ZERO kh6n explicit fail-loud rejects remain in `instructions.jl` — every LLVM round/min/max intrinsic dispatches natively. Below: **Bennett-mq6f close** — corrective follow-up to kh6n + k2w6. New `soft_round_away` primitive (round-half-AWAY-from-zero, ≡ `llvm.round.f64` per LLVM langref). LLVM dispatch in `src/extract/instructions.jl`: explicit `llvm.round.` arm → `soft_round_away` (was silently miscompiled by no-op-arm fallthrough to banker's `soft_round`); explicit `llvm.roundeven.` arm → `soft_round` (replaces kh6n explicit-reject — both are banker's, kh6n misstatement corrected inline). 7367 / 7367 asserts green incl. tie sweep × N=0:10, 1074-binade subnormal sweep, 5000-input random sweep vs `RoundNearestTiesAway`, IR ingest dispatch + f32 reject. Below: **Bennett-k2w6 close** — native `soft_fmin` / `soft_fmax` (NaN-absorbing ≡ `llvm.minnum` / `llvm.maxnum`) + `soft_fminimum` / `soft_fmaximum` (NaN-propagating ≡ `llvm.minimum` / `llvm.maximum` ≡ Julia `Base.min`/`Base.max` bit-exactly). Closes Bennett-kh6n's `soft_fmin/fmax future work` stub. Built on `soft_fcmp_olt` + explicit `±0` sign-tie-break + NaN dispatch override. LLVM dispatch in `src/extract/instructions.jl` replaces the four kh6n float-rejects with native `IRCall`. `Base.min`/`Base.max` overrides routed to NaN-propagating pair. New `_CALLEES_FP_MINMAX` group. 434,472 / 434,472 asserts green (100k random pairs × 4 primitives + ±0/NaN/±Inf matrix + 1074-binade subnormal-input sweep × 4 + IR + SoftFloat dispatch). Below: **Bennett-kh6n close** — defensive codemod tightening every `startswith(cname, "llvm.<x>")` in `src/extract/instructions.jl` (32 sites) with trailing `.`, mirroring `vectors.jl`'s discipline. Plus explicit fail-loud rejects for `llvm.minimumnum.*` / `llvm.maximumnum.*` (LLVM 19+, swallowed pre-fix; **superseded by p19b native dispatch above**) and `llvm.roundeven.*` (banker's rounding, swallowed pre-fix; **superseded by mq6f native dispatch above — kh6n misstatement about `soft_round` semantics also corrected**). 13/13 new + 6981 regression asserts green.
>
> **Then read [`worklog/065_2026-05-14_k31q_check_bounds.md`](worklog/065_2026-05-14_k31q_check_bounds.md)** — 2026-05-15 (top): **Bennett-8ygo + Bennett-bipw close** — symmetric log1p refactors of `soft_atanh` (drops K=25 polynomial; (a) `0.5·log1p(2|x|/(1-|x|))` for `|x|<0.5`, (b) old log form for `0.5≤|x|≤1`, `|x|>1→NaN`) and `soft_acosh` (drops K=15 polynomial with `s²=2(x-1)` scaffold; (b) `log1p(t+sqrt(2t+t²))` for `1≤x<2`, (c) `log(2x-1/(x+sqrt(x²-1)))` for `2≤x<2^28`, (d) `log(x)+ln(2)` for `x≥2^28`, `x<1→NaN`). 2087/2087 + 33/33 dispatch (atanh); 4492/4492 + 27/27 dispatch (acosh). Below: **Bennett-tfmo close** — `soft_asinh` to Julia stdlib's four-regime natural formula (log1p-based), dropping 30 polynomial coefficients + 60 Horner-Taylor ops in favour of 13 soft-float ops. 0 ULP across 26 strategic spot-checks; 5178/5178 + 31/31 dispatch. Then **Bennett-9wmk attempt+revert**: bare mid-algorithm fast_copy swap saves ~25% wire but adds 30% depth — breaks polylog-depth promise — bead left OPEN with finding. Then **Bennett-gsxe close**: Cuccaro 2W−2 → 2W−3 Toffoli via §3.5 high-bit fold. Then 2026-05-14 **Bennett-k31q close**: NOT test-ordering — `--check-bounds=yes` provokes a GC frame in `cond_pair`.
>
> **Then read [`worklog/064_2026-05-13_project_orientation_beads.md`](worklog/064_2026-05-13_project_orientation_beads.md)** — 2026-05-13/14: orientation session, then the 2026-05-14 ao66 follow-up tidy (trailing-`.` discipline, 3+1 record correction), `runtests.jl` `runfile` per-file progress markers + `BENNETT_HEAVY_TESTS` gate, and the `test_kmuj_callee_groups.jl` stale-count fix (`_CALLEES_FP_TRANS` 19→27). The full `Pkg.test()` is **~27 min** (~10-12 min with `BENNETT_HEAVY_TESTS=0`) — CLAUDE.md's "~5 min" figure is stale.
>
> **Then read [`worklog/063_2026-05-06_g82n_soft_atanh_TIER_C1_COMPLETE.md`](worklog/063_2026-05-06_g82n_soft_atanh_TIER_C1_COMPLETE.md)** — 2026-05-07, top entry: **Bennett-o7cy close** (Tier C2.2 — `soft_expm1` + dispatch for `llvm.expm1.f64` and libm `@expm1`). **Second C2 transcendental, symmetric to log1p.** Three-regime branchless: tiny |x|<2^-54 → x; K=15 Taylor for |x|≤0.5 (exact rational coeffs `c_k=1/k!`); `exp(x)-1` directly for medium (handles ±Inf/NaN/large-neg→-1/large-pos→+Inf naturally). ONE soft_exp_fast call. ≤2 ULP across 300k random × 3 seeds; §13 0 ULP across all subnormals. _CALLEES_FP_TRANS extended 26 → 27. **Drive-by fix: tightened `llvm.exp.` and `llvm.exp2.` prefixes** with trailing-`.` discipline (pre-fix `llvm.exp` arm silently swallowed `llvm.expm1.f64` — same bug class as the log1p fix earlier in the session). **Two prefix-discipline bugs in one session — going forward, every `startswith(cname, "llvm.<name>")` MUST include trailing `.` from the moment of insertion.** Below it: **Bennett-0ulc close** (Tier C2.1 — `soft_log1p`) (Tier C2.1 — `soft_log1p` + dispatch for `llvm.log1p.f64` and libm `@log1p`). **First C2 transcendental close.** Two-regime branchless port: `|x| < 2^-54 → x` bit-exactly (subnormal preserved); medium uses Julia stdlib's precision-recovery formula `log(1+x) + (x - ((1+x)-1))/(1+x)` to recover bits lost in `1+x` rounding. ≤2 ULP vs `Base.log1p` on 300k random × 3 seeds (actually max 1 ULP). _CALLEES_FP_TRANS extended 25 → 26. **Highest-leverage C2 entry**: enables future cleanup of asinh/acosh/atanh polynomial regimes (K=15-30 → K=8). **Initial draft tiny-threshold bug** documented (used 2^-26 — wrong, gave 2.4M ULP at x=1e-9; correct threshold is 2^-54 derived empirically + algebraically). Below it: **Bennett-g82n close** (Tier C1.11 — `soft_atanh` + dispatch for `llvm.atanh.f64` and libm `@atanh`). **TIER C1 COMPLETE 11 of 11.** Three-regime branchless port: `|x| > 1 → NaN` / K=25 polynomial in z=x² for `|x| ≤ 0.5` (exact rational coefficients `c_k = 1/(2k+1)`) / `0.5·log((1+|x|)/(1-|x|))` for medium `0.5 < |x| ≤ 1`. atanh is ODD; ±1 → ±Inf via natural log propagation. ≤2 ULP on 300k random × 3 seeds; §13 0 ULP across all 1074 subnormal binades × ±. _CALLEES_FP_TRANS extended 24 → 25. **Six bead closures in this session: m2bv tanh → ky5n sinh → bybh cosh → sfx9 asinh → eq9p acosh → g82n atanh.**
>
> **Then read [`worklog/062_2026-05-06_sfx9_soft_asinh.md`](worklog/062_2026-05-06_sfx9_soft_asinh.md)** — 2026-05-06, top entry: **Bennett-eq9p close** (Tier C1.10 — `soft_acosh` + dispatch for `llvm.acosh.f64` and libm `@acosh`). **Fifth hyperbolic primitive**, FIRST domain-restricted (x < 1 → NaN). 10 of 11 Tier C1 done. Four-regime branchless port: domain check / K=15 polynomial via `s²=2(x-1)` substitution for `1 ≤ x ≤ 1.3` / `log(x + sqrt(x²-1))` medium / `log(x) + ln(2)` huge. `s²`-substitution factors out the essential sqrt-singularity at x=1 (kernel converges fast on `[0, ~2]`). Polynomial threshold pushed to 1.3 (vs analytical 1.05) to give the medium arm safety margin against soft-float rounding accumulation. ≤2 ULP on 300k random × 3 seeds; **§13 contract DIFFERENT (domain-restricted)**: `soft_acosh(any subnormal) = NaN`. _CALLEES_FP_TRANS extended 23 → 24. Below it: **Bennett-sfx9 close** (Tier C1.9 — `soft_asinh` + dispatch for both `llvm.asinh.f64` and libm `@asinh`). **Fourth hyperbolic primitive**. Three-regime branchless port adapting Julia stdlib `Base.asinh` with `log1p` substituted by extended polynomial regime (Bennett.jl lacks `soft_log1p` — empirically `log(|x|+sqrt(x²+1))` loses ~16M ULPs at |x|=1e-9 because `soft_log` near 1 can't recover the small-perturbation precision that Julia stdlib's `log1p` does). Solution: K=30 polynomial in z=x² covers `|x| ≤ 0.55` (slow Taylor convergence due to branch points at ±i — radius 1; covering 0.55 needs K=30); `log(|x|+sqrt(x²+1))` for medium 0.55 < |x| < 2^28; `log(|x|) + ln(2)` for huge. ONE soft_log call via regime-selected arg, ONE soft_fsqrt. ≤2 ULP across 300k random × 3 seeds; **§13 0 ULP across all 1074 subnormal binades × ±**. _CALLEES_FP_TRANS extended 22 → 23. **Filed as future work**: `soft_log1p` sibling primitive — would let asinh/acosh/atanh use the natural log1p formula at far lower polynomial degree (~K=8 vs K=30). Possibly file before starting atanh/acosh.
>
> **Then read [`worklog/061_2026-05-06_ky5n_soft_sinh.md`](worklog/061_2026-05-06_ky5n_soft_sinh.md)** — 2026-05-06, top entry: **Bennett-bybh close** (Tier C1.8 — `soft_cosh` + LLVM-ingest dispatch for both `llvm.cosh.f64` intrinsic and libm `@cosh`). **Third hyperbolic primitive**. Three-regime branchless port mirroring ky5n with three localised differences (cosh is even, medium is sum `(E+1/E)/2` so zero cancellation, polynomial has no `x` factor). Polynomial coefficients verbatim from `Base.cosh_kernel`. ONE `soft_exp_fast` call via regime-selected arg. ≤2 ULP across 100k random × 3 seeds × 5 buckets; **§13 contract DIFFERENT for cosh: `cosh(any subnormal) === 1.0` bit-exactly** across all 1074 binades × ±. **3+1 protocol skipped** per §2 exception. Below it: **Bennett-ky5n close** (Tier C1.7 — `soft_sinh` + LLVM-ingest dispatch for BOTH `llvm.sinh.f64` intrinsic AND libm `@sinh`). **Second hyperbolic primitive**. Three-regime branchless port: degree-8 polynomial in `z=x²` for `|x| ≤ 1.0` (Julia stdlib `Base.sinh_kernel` minimax coefficients), `(E - 1/E)/2` with `E = exp(|x|)` for `1 < |x| < 709`, `(0.5·E)·E` with `E = exp(|x|/2)` for `|x| ≥ 709`. **ONE `soft_exp_fast` call total** via regime-selected argument. 3+1 synthesis pivoted from Proposer B's unified `(E²-1/E²)/2` (which had 5 chained fmul ops → 3-4 ULP at |x|≈1.4) to A's regime-split `(E-1/E)/2` (3 ops after exp → ≤2 ULP). §13 subnormal-INPUT bit-exact (0 ULP across 1074 binades × ±). Drive-by finding: `soft_exp_fast` has a small NaN-producing bug for inputs in `(~709.78, ~709.79)`; worked around by setting Bennett-ky5n's huge-regime threshold conservatively at 709.0 (vs Julia's 709.7822265633563). CRITICAL load-bearing operator ordering: `(0.5·E)·E` not `(E·E)·0.5` for the huge arm. `_CALLEES_FP_TRANS` extended 20 → 21. **`cosh` is the next pickup** (Tier C1.8 — even function, `(E+1/E)/2`, can share huge-arm infrastructure with sinh).
>
> **Then read [`worklog/060_2026-05-06_m2bv_soft_tanh.md`](worklog/060_2026-05-06_m2bv_soft_tanh.md)** — 2026-05-06, **Bennett-m2bv close** (Tier C1.6 — `soft_tanh` + LLVM-ingest dispatch for BOTH `llvm.tanh.f64` intrinsic AND libm `@tanh` external call). **First hyperbolic primitive** in Tier C1. Branchless port of Julia stdlib `Base.tanh(::Float64)` — three regimes: degree-10 polynomial in `x²` for `|x| ≤ 0.5` (coefficients verbatim from `Base.tanh_kernel`), `1 - 2/(exp(2|x|)+1)` for medium, `±1` saturation for `|x| ≥ 22`. ONE `soft_exp_fast` call total. ≤2 ULP vs `Base.tanh` on 100k random × 3 seeds × 4 magnitude buckets; §13 subnormal-input bit-exact across all 1074 binades × ±.
>
> **Then read [`worklog/059_2026-05-06_7goc_soft_atan2.md`](worklog/059_2026-05-06_7goc_soft_atan2.md)** — 2026-05-06, **Bennett-7goc close** (Tier C1.5 — `soft_atan2` + LLVM-ingest dispatch for BOTH `llvm.atan2.f64` intrinsic AND libm `@atan2` external call). Faithful musl `atan2.c` port built on `soft_atan` per CLAUDE.md §12 — ONE soft_fdiv + ONE soft_atan + ONE soft_fsub; quadrant offset via XOR / ifelse. ≤2 ULP vs `Base.atan(y, x)` on 100k random × 3 seeds × 4 magnitude buckets × 4 quadrants; all special cases bit-exact (axis points, ±0/±0, ±Inf/±Inf, NaN propagation). `_CALLEES_FP_TRANS` extended 18 → 19. **Drive-by fix**: tightened all six trig prefixes (`llvm.{sin,cos,tan,atan,asin,acos}.`) with trailing `.` to fix the silent-miscompile pre-7goc — `startswith("llvm.atan")` matched `llvm.atan2.f64` and dispatched to soft_atan(y) dropping x; `atan2(3,4)` returned `atan(3) ≈ 1.249` instead of `0.6435`. Tier C1 now 5 of 11 done; **`tanh` is the next pickup** (needs `soft_exp` already done; use `(e^{2x}-1)/(e^{2x}+1)` form). Investigation finding: Julia `Base.atan(y, x)` does NOT lower to `llvm.atan2.f64` NOR libm `@atan2` — it inlines into Julia primitives + recursive 1-arg `j_atan` call; the `.ll`-ingest dispatch path is the only one that works for all Tier C1 transcendentals (the SoftFloat-typed Julia-source path is broken across the family because no `Base.{sin,cos,tan,atan,asin,acos,atan2}(::SoftFloat[, ::SoftFloat])` overrides exist — separate followup).
>
> **Then read [`worklog/058_2026-05-04_ckvj_soft_asin.md`](worklog/058_2026-05-04_ckvj_soft_asin.md)** — 2026-05-04 / 05, **two consecutive closes**: top-of-chunk **Bennett-bd7f close** (Tier C1.4 — `soft_acos` + `llvm.acos` dispatch; faithful musl `acos.c` port; **REUSES `_asin_R` + 10 R-coefficients + `pio2_hi`/`pio2_lo` from `fasin.jl` per CLAUDE.md §12**; ≤2 ULP vs `Base.acos` on 100k random × 3 seeds; subnormal-output proven absent per §13). Below it: **Bennett-ckvj close** (Tier C1.3 — `soft_asin` + `llvm.asin` dispatch; faithful musl `e_asin.c` port; ≤2 ULP vs `Base.asin` on 100k random × 3 seeds; 1075-input subnormal binade sweep at 0 ULP; gotcha #1 = `Base.asin` THROWS on OOB not NaN, gotcha #2 = Path C₂'s 0/0 at |x|=1 masked by override ordering, gotcha #3 = R(z) helper shared with acos). `_CALLEES_FP_TRANS` extended 16 → 17 → 18.
>
> **Then read [`worklog/057_2026-05-04_s1zl_soft_tan.md`](worklog/057_2026-05-04_s1zl_soft_tan.md)** — 2026-05-04, two entries top-down: **Bennett-qpke close** (Tier C1.2 — `soft_atan` + `llvm.atan` dispatch; faithful musl `atan.c` branchless port, self-contained no `_rp_rem_pio2` dependency; max ULP ≤ 2 on 500k random + 1076-input subnormal binade at 0 ULP; gotcha #1 = SLP-vectorisation trap on parallel `soft_fdiv` calls, fix is one fdiv on ifelse-selected num/den). Below it: **Bennett-s1zl close** (Tier C1.1 — `soft_tan` + `llvm.tan` dispatch; musl `__tan` port reusing `_rp_rem_pio2` from `fsin.jl`; max ULP = 1 on 500k random samples; SET_LOW_WORD odd-arm precision trick from musl).
>
> **Then read [`worklog/056_2026-05-03_37mt_memcpy_phase1.md`](worklog/056_2026-05-03_37mt_memcpy_phase1.md)** — 2026-05-03 late evening, top entry: **Bennett-8bys research/scope-split + Bennett-munq close**. Empirical scan of t5_tr2_hashmap.ll: 60 memcpy sites, ALL using `alloca [N x i8]` ArrayType (silently dropped pre-munq) — Phase 1+2 covered ZERO of them without ArrayType extraction. 8bys split into 6 sub-beads (munq P2; ixiz/xtu9/yxr8/zmry/doih all P3); 8bys kept open as umbrella. Bennett-munq close: ~30 LOC extension to `_alloca_elem_width_bits` + alloca dispatch in `instructions.jl`; 61 testset asserts; 3+1 deviation documented (mechanical extension, design space catalogued by corpus survey). 1186 asserts green in targeted sweep. Below it: **Bennett-9nwt close** (Bennett-hao Phase 2): const-c const-N memset on alloca-i8-backed dst. `_handle_memset_arm` + `_alloca_is_fresh` (~210 LOC) added; `llvm.memset` removed from benign list. 13-predicate cascade with two no-op short-circuits (N=0, c=0). 82 testset asserts / 9 testsets (3 green case A + C, 6 reject). 3+1 synthesis: option γ (intra-block freshness sweep) for c≠0 path, option β (preserve pre-9nwt broad tolerance) for c=0 fast-path. Below it: **Bennett-37mt close** (Phase 1: const-size memcpy): const-size memcpy between distinct `alloca i8`-backed pointers lowers to N byte-granular IRPtrOffset+IRPtrOffset+IRLoad+IRStore quads. `src/extract/instructions.jl` gains `_handle_memcpy_arm` + helpers `_alloca_root_ref` / `_alloca_elem_width_bits` (~155 LOC). 8 testset / 80 assertions across 3 green paths (N=4/8/24, identity oracle clean for `Int8(-8):Int8(8)`) + 5 reject paths (volatile / variable-N / same-alloca / alloca-i64 elem_w mismatch / memmove). Big gotcha: the bead's "(N/8) at 64-bit granularity, memory.jl unchanged" wording was internally inconsistent — `aggregate.jl:227` (`ew == 8 || continue`) and `memory.jl:245-246` (`inst.width == elem_w`) force byte-granular chunks on `alloca i8`. Honest scope tightening, not creep. hao still open; closes when 9nwt + 8bys close.
>
> **Then read [`worklog/055_2026-05-03_3mo_sin_cos.md`](worklog/055_2026-05-03_3mo_sin_cos.md)** — 2026-05-03, **Bennett-3mo close**: full musl `sin.c` / `cos.c` / `__rem_pio2.c` / `__rem_pio2_large.c` port with full Payne-Hanek argument reduction. `src/softfloat/fsin.jl` (~770 LOC) houses both `soft_sin` and `soft_cos`; LLVM dispatch entries for `llvm.sin.*` / `llvm.cos.*` route raw .ll/.bc ingest to them. **100k random sweep (3 seeds, 5 magnitude buckets up to 1e22): cos max ULP = 0, sin max ULP = 1, 0 fails > 2 ULP.** 1076-input subnormal-INPUT sin sweep bit-exact. soft_sin compiles to ~11M reversible gates (verified, all simulate inputs bit-exact vs Base.sin). Big gotcha: native UInt128 in compile-to-gates code emits i128 constants which Bennett rejects (Bennett-l9cl) — even though Bennett-yys3 said UInt128 ops compile. The (hi, lo) UInt64-pair representation is mandatory for paynehanek + fromfraction. Five other gotchas captured at the chunk top. _CALLEES_FP_TRANS extended 12 → 14.
>
> **Then read [`worklog/054_2026-05-02_soft_log.md`](worklog/054_2026-05-02_soft_log.md)** — 2026-05-02, top entry: **Bennett-jexo close** (`soft_pow_julia` Path A — line-for-line port of `Base.:^(::Float64, ::Float64)` using Julia's `_log_ext` + extended `exp_impl(x, xlo)` kernels; `Base.:^(::SoftFloat, ::SoftFloat)` routes to it; LLVM `llvm.pow.f64` dispatch keeps using `soft_pow` musl for raw .ll/.bc ingest; **300,000 / 300,000** random samples bit-exact vs `Base.:^`; integer-y bug pinned and fixed via `muladd→mul+add` distinction at `@noinline` sites). Below it: **Bennett-582 + Bennett-emv close** — branchless ports of Arm Optimized Routines / musl: `flog.jl` (~310 LOC, log + log2 + log10), `fpow.jl` (~700 LOC, soft_pow + soft_powi). soft_log: 100% bit-exact vs `Base.log` on 100k samples. soft_pow: 99.17% bit-exact, 0% above 2 ULP across 13k extreme bivariate samples, tracks musl/glibc pow via ccall. LLVM dispatch entries for `llvm.log` / `llvm.log2` / `llvm.log10` / `llvm.pow.f64` / `llvm.powi.f64.i32`; Float32 forms rejected per §13. Per-bead regression tests + `.ll` fixtures ship alongside. _CALLEES_FP_TRANS extended 6 → 12.
>
> **Then read [`worklog/053_2026-05-01_dnh_research_nj6c.md`](worklog/053_2026-05-01_dnh_research_nj6c.md)** — 2026-05-01 late late evening, four entries top-down: **Tier A grind: Bennett-h6f + Bennett-4eu + Bennett-imz7 closed** (`llvm.fma`/`fmuladd` direct dispatch via existing `soft_fma`; `indirectbr` declared a Bennett hard stop with precise fail-loud error; ~24 vpch sites tightened from generic `error()` to typed exceptions across 8 src files + 9 test files; Pkg.test 487,167 pass), then **Bennett-cb9y + Bennett-dnh close → OPCODE PARITY for runtime-indexed memory** (multi-origin × runtime-idx walls closed at `memory.jl:141` and `aggregate.jl:362` via `extern_pred_wire` kwarg + synthetic-IRLoad pattern; covers MUX-EXCH AND shadow_checkpoint multi-origin shapes; `Bennett-dnh` itself closes — every load/store/GEP at runtime SSA index compiles, single-origin OR multi-origin, all shapes; Pkg.test 487,151 pass / 0 fail / 3 broken), then **Bennett-nj6c close** (dnh phase 1a — `_MUX_SHAPES_NW` extended to all (N,W) with N·W ≤ 64), then **Bennett-dnh research/re-scope** (six-agent research sweep on 2026-05-01 evening). Bennett's LLVM memory-opcode coverage now matches Enzyme's (modulo correctly-hard-stop atomics/EH). Open follow-ups (NOT opcode gaps): **Bennett-8guh** (IRSwap, P2, 3+1) and **Bennett-6c6f** (QROAM cost win, P3). Below it in chunk 052: **Bennett-1pb close** (direct `llvm.sqrt.f64` / `llvm.exp.f64` / `llvm.exp2.f64` dispatch), **Bennett-kv7b / U65 close** (test-coverage epic), **Bennett-i2ca / U55 close** (`bennett(lr; strategy=...)`).
>
> **Then read [`worklog/051_2026-05-01_eleven_close_grind.md`](worklog/051_2026-05-01_eleven_close_grind.md)** — 2026-05-01 afternoon grind, **11 catalogue closes** in one session, hardest-first: **iwv5** (U90, softfloat/persistent → `module SoftFloatLib` / `module Persistent`); **u71l** (U161, `CompileOptions` struct + opts-form `reversible_compile` overloads); **x2iw** (U88, `BlockLoweringOpts` bundle replaces 11 kwargs on `lower_block_insts!` / `lower_loop!`); **u2yp** (U149, drop sat_pebbling.jl + PicoSAT dep); **vpch** (U45, ~146 `error()` → `ArgumentError` / `DimensionMismatch` / `AssertionError` codemod across 29 src files); **3rph** (U137, Float32 double-rounding deviation documented; native f32 filed as Bennett-e283); **gk1h** (U210, Aqua.jl + JET.jl hygiene gates); **fidj** (U217, `:auto` × liveness invariance pinned); **58rl** (U214, dolt-cache commit-bundling convention documented); **sa39** (U211, BenchmarkTools-based compile-time bench); **8403** (U159, per-source-file unit-test homes for Bennett.jl/lower.jl/ir_extract.jl + PER_SOURCE_INDEX.md). All 419291+ tests pass; gate counts unchanged. **Catalogue is now 1.7 % open** (3 of 173 beads): 25dm blocked on z2dj, i2ca needs 3+1, kv7b epic ongoing.
>
> **Then read [`worklog/050_2026-05-01_v958_ehoa_lm3x.md`](worklog/050_2026-05-01_v958_ehoa_lm3x.md)** — 2026-05-01 morning grind, 4 catalogue closes: **s92x** (U115, SretInfo struct + _collect_sret_writes decomposition); **lm3x** (U56, MUX load/store @eval consolidation, ~110 LOC deleted, single `_MUX_SHAPES_NW` source); **ehoa** (U43, LoweringCtx ::Any → concrete dict types); **v958** (U68, IROperand tagged-union → abstract type hierarchy via 3+1 protocol with 4 sentinel singletons, 47 `.kind` sites eliminated). All 419,228 tests pass, gate counts unchanged at every site.
>
> **Then read [`worklog/049_2026-04-30_vdlg_x3jc_zpj7_kv7b.md`](worklog/049_2026-04-30_vdlg_x3jc_zpj7_kv7b.md)** — 2026-04-30 grind, three structural splits + four kv7b sub-items shipped: **vdlg** (`lower.jl` 3,172 → 9 files under `src/lowering/`), **x3jc** (`ir_extract.jl` 2,946 → 9 files under `src/extract/`), **zpj7** (5 pebbling/eager files → `src/pebble/`), kv7b sub-items #05 F19 (loop tests Int8 → all widths), #03 F8/F15/F16 + #05 F15 (SHA-256 / mul-dispatcher / vector_ir tests sampled → exhaustive). Net +263k test asserts.
>
> **Read [`worklog/048_2026-04-27_1xub_persistent_ds_refresh.md`](worklog/048_2026-04-27_1xub_persistent_ds_refresh.md) NEXT** — top entry is the **2026-04-28 LOC-tier grind summary** (**51 beads closed**; net -16,700 LOC). Substantive 30: qxg9 P2 perf-bug fix at 33× compile-time speedup; P3 fehu simulate! 2.7× speedup, 2hhx soft_round, is5s diagnose_nonzero, g7r8 regression check, **19g6 Bennett.jl 545→270 LOC split**, plus 9 more; P4 incl. xgf6 BENCHMARKS -42%/-34%, mg6u/s8gs/nj5r polish, 4nvl gitignore -17,818 LOC, plus 6 stale-bead closes. Plus 21 LLVM-opcode/intrinsic verify-or-fail-loud closes. Plus kv7b epic partial (5 of 19 sub-items, +11 test asserts). Below it: the 2026-04-28 qxg9 close detail, then 2026-04-27 qxg9 bisect data and 1xub close. Then [`worklog/047_2026-04-27_heup_fold_constants_doc_only.md`](worklog/047_2026-04-27_heup_fold_constants_doc_only.md) (7 prior 2026-04-27 entries: tzrs stage-1 → tbm6 → 7xng → cvnb → jc0y → q04a → heup).
>
> **State of the bug backlog (2026-04-28):** qxg9 closed today. Only 3 `[bug]` beads remain: `25dm` (P2, blocked on `z2dj` IN-PROGRESS T5-P6 dispatcher), `ponm` (P2, bd-tool schema bug — NOT a Bennett.jl source bug), `cc0.5` (P2 IN-PROGRESS — Julia TLS allocator GEP base, T5-P6.3 multi-language ingest, multi-session scope per its own notes).
>
> **Active mode: structural / LOC refactors.** User explicitly lifted the bugs-only directive 2026-04-27 evening. Today's grind (2026-04-28) closed **51 beads** + kv7b partials, net -16,700 LOC — see chunk 048 top entry. Yesterday's (2026-04-27) closes:
> - `7xng` — LoweringResult.constant_wires dead-store removal (-45 LOC)
> - `cvnb` — bennett_direct + self_reversing discoverability (Sturm.jl-ao1)
> - `tbm6` — Karatsuba multiplier removed (~250 LOC delete)
> - `tzrs` stage-1 — `_handle_intrinsic` extracted via 3+1 protocol (still OPEN; stages 2-5 deferred per CLAUDE.md §11)
>
> **Active rule of thumb:** any change to `lower.jl` / `ir_extract.jl` / `bennett_transform.jl` / `gates.jl` / `ir_types.jl` / phi resolution → **3+1 protocol per CLAUDE.md §2** (2 parallel `Plan` proposers, synthesise, implement, review). Otherwise → direct grind. Always pair `verify_reversibility` with an output-vs-Julia-oracle assertion — `verify_reversibility` does NOT check semantic correctness (see today's 3of2 close + the chunk-045 directive).
>
> **Suggested next pickups** (all P2/P3 non-bug, most need 3+1 because they touch core files):
>   - `vdlg` (P2) — lower.jl 2,662 LOC structural split
>   - `tzrs` stages 2-5 — remaining `_convert_instruction` arms (medium priority; stage 1 already shipped most of the value)
>   - `x3jc` (P3) — ir_extract.jl 2,394 LOC similar to vdlg
>   - `ehoa` second-half (P2) — `LoweringCtx` `::Any` field concretization (the back-compat-ctor half of ehoa was closed as a drive-by during tbm6)
>   - `s92x` (P3) — `_detect_sret` 173-line soup
>   - `vt0a` (P3) — Bennett-aware wire allocator (algorithmic; needs careful design)
>
> Smaller / contained next-tier work (no 3+1):
>   - `qjet` — empirical timing-based reorder of test/runtests.jl (carries test-ordering risk)
>   - `19g6` — src/Bennett.jl 297-line junk drawer (refactor target ≤80 LOC)
>   - `iwv5` — wrap softfloat/persistent in proper modules (touches namespace; high blast radius)
>   - `zpj7` — pebbling/eager file naming consistency
>   - `2hhx` — soft_round (roundToIntegralTiesToEven) implementation
>   - `3rph` — Float32 native arithmetic (currently fpext→f64→fptrunc)
>   - `u2yp` — sat_pebbling.jl: wire (via fg2) or drop the PicoSAT dep
>   - `u71l` — CompileOptions struct (565 callers; deferred 2026-04-28, needs careful design)
>
> Older session logs are sharded into ~200-300 line chunks under `worklog/`, file-numbered chronologically (000 = oldest, 047 = newest as of 2026-04-27).

This file is now an **index** — historical content was sharded out of the
monolithic 9,774-line `WORKLOG.md` per Bennett-fyni / U70. Concatenating the
chunk files in REVERSE filename order (`037 → 036 → … → 000`) reproduces the
original byte-for-byte; `scripts/shard_worklog.py` is the canonical re-shard
tool.

## Adding new entries

When you finish a session, **prepend** a `## Session log — YYYY-MM-DD — …`
block to the current top chunk file (today: `worklog/038_*.md`). When that
file passes ~280 lines, create a new chunk file with the next sequential
`NNN_` prefix and start prepending there. The next agent should always be
able to read the highest-numbered file first to find the latest state.

Re-running `python3 scripts/shard_worklog.py` re-flows everything if structure
drifts; it verifies byte-for-byte that the reverse-concatenation matches the
sum of all chunk files.

## Session-log template (per Bennett-fyni / U70)

Separate "what shipped" (already in git log) from durable knowledge (gotchas,
rejected paths, hand-off pointers). Avoid restating the diff:

```markdown
## Session log — YYYY-MM-DD — Bennett-<id> / U## <one-line summary>

**Shipped:** see git log around <commit-sha>; <one-line summary of intent>.

**Why:** <motivation / context not derivable from the diff>

**Gotchas / Lessons:** <surprising findings worth keeping for future sessions>

**Rejected alternatives:** <approaches tried and why they didn't work>

**Next agent starts here:** <one-line pointer to the next concrete step>
```

## Index — newest first

| File | Lines | First section |
|---|---:|---|
| [076_2026-05-28_vision_north_north_star.md](worklog/076_2026-05-28_vision_north_north_star.md) | 56 | Session log — 2026-05-28: **Vision PRD north-north-star.** Folded the broader taint-driven quantum-compiler-toolchain vision into `Bennett-VISION-PRD.md` (new top blockquote + §1.1 larger picture + §1.2 reversible-first operating order; §10 Non-Goals reconciled). Bennett.jl + BennettVM = classical-oracle-synthesis core; our scope = "from the tainted LLVM opcodes on down" (front-end taint marking/propagation owned upstream); litmus = identity on untainted code ("compile Linux for the quantum VM"). Operating order: reversible-first, then recycle results/learnings for quantum — near-term roadmaps unchanged. Quantum walls recorded for later (reversible≠unitary; tainted control flow ≠ classical PC; uncomputation must disentangle). Doc-only; 3+1 N/A. |
| [074_2026-05-22_5ikt_heap_m3.md](worklog/074_2026-05-22_5ikt_heap_m3.md) | 239 | Session log — 2026-05-22: **Bennett-bd5f close (gf3n Milestone M4) — gf3n EPIC COMPLETE.** M4 = fail-loud scope hardening: new `_m4_error` + `_m4_scope_guard` in `src/extract/heap.jl` — a callee-name scan run early in `_detect_gc_preamble!` (anchored regexes, callee-names-only) that rejects `Dict` (`j_setindex!` → precise message + `Bennett-800b` pointer), mid-array `insert!`/`deleteat!` (`j__growat!`/`j__deleteat!`), and C/Rust `malloc`/`calloc`/`realloc` with precise signpost messages; the M1 back-edge message sharpened. Purely additive — every case already rejected loud pre-M4, M4 only sharpens the message. Recon manglings were wrong (re-derived from real IR: `j_setindex!_NNN` not `j_setindex!##NNN`, `j__growat!` not `j_#_insert!`). Non-bits element types found genuinely SUPPORTED (`Array{Tuple{Int8,Int8}}` compiles) — a blanket reject would false-positive, so NOT added (STOP-and-report). New `test/test_bd5f_heap_m4.jl` 13/13 (message-pinned) + `heap_m4_malloc.ll`; corpus TJ2/TJ4 comments updated (Bennett-800b / Bennett-890r). Peer regressions byte-identical: M1 519/519, M2 9734/9734, M3 529/529, gate-count 39/39. **Below it: Bennett-5ikt close (gf3n Milestone M3)** — a `push!`-built `Vector{T}` with a statically-inferable element count now compiles under `mem=:heap`; corpus fixture **TJ1** (`Int8[]` + push×3 + `reduce(+,v)`) is genuinely green (326 gates, oracle `3x+3`). New M3 block in `src/extract/heap.jl` (~+770 LOC): `_recognise_growend_array` builds the `_HeapArray` structurally (no Memory `gc_small_alloc` — empty `Int8[]` reuses Julia's shared empty-Memory global; the only alloc is the Array wrapper); `_collapse_growend_diamond` collapses each `@j_#_growend!` capacity diamond (non-mutating); `_infer_growend_capacity` derives N from the dense element-store offset prefix; `_m3_skeleton` rebuilds the taint closure with element-data loads as SINKS (the D3 fix — without it the `reduce` adds taint into SKEL and the return is silently dropped; backstopped by M2-O3); `_prove_growend_partition_sound` runs M2 O1-O7 + M3 G1/G3/G4. Routing: the `has_growend` branch discriminates `heap_live` → M3, else unchanged M1. 3+1 — orchestrated (sonnet recon → 2 `Plan` architects → synthesis → opus implementer split refactor+core → review +1 → 2 fixup passes); the implementer re-extracted the IR and caught 3 recon divergences (STOP-and-report); review found obligation G2 dead (subsumed by M2-O5 once the `data_roots` refactor generalised O5) and deleted it. New `test/test_5ikt_heap_m3.jl` 529/529 + 4 `.ll` reject fixtures; TJ1 `@test_throws`→green oracle. Peer regressions byte-identical: M1 519/519, M2 9734/9734, gate-count 39/39. Next: M4 `Bennett-bd5f` (fail-loud hardening + corpus reconciliation; standard review, not 3+1). |
| [073_2026-05-21_gps7_heap_m1.md](worklog/073_2026-05-21_gps7_heap_m1.md) | 226 | Session log — 2026-05-21: **Bennett-kuza close (gf3n Milestone M2)** — `Array{T}(undef,const_N)` heap integer arrays whose element data is LIVE into the return now compile under `mem=:heap`. M2 extends `src/extract/heap.jl` (~+1100 LOC): `_detect_gc_preamble!` forks M1 (fully-dead skeleton → drop) vs M2 (partially-dead → re-root); the M2 path recognises the heap array (1 alloc = Memory only / 2 = Memory + Array wrapper), partitions M1's `SKEL` into {dead GC machinery → drop} ∪ {live element traffic → re-root}, runs a 7-obligation bail-loud soundness proof, collapses the Julia bounds-check diamond, and re-roots element stores/loads onto a synthetic `IRAlloca(_,8,ConstOperand(N))` compiled by the existing `:shadow`/`:mux`/`:shadow_checkpoint` lowering. No new IR node, no lowering change. 3+1: 2 `Plan` architects diverged on the bounds-check diamond (A: straight-line-only, reject the diamond; B: collapse it) — orchestrator chose B (B proved a runtime-indexed non-SROA'd heap array NECESSARILY carries the diamond — the diamond is what blocks SROA; straight-line-only M2 compiles nothing real). Review fixes: reject runtime-index element stores (out of consensus §4 M2 scope — filed follow-up `Bennett-5006`); handle `_convert_instruction`→nothing. New `test/test_kuza_heap_m2.jl` 9734/9734 + `test/fixtures/heap_m2_cond_pair.ll`; M1 519/519 byte-identical; gate-count 39/39. Gotchas: only N≥256 keeps a heap skeleton (N≤64 SROAs to a stack alloca); g256 oracle must sweep only written slots (`mod(i,3)` not `mod(i,100)`); M1-vs-M2 routing is on `growend!` presence; the M2-aware ret-ancestor walk re-roots element traffic (stops at `data_ptr`). Next: M3 `Bennett-5ikt`. **Below it: Bennett-gps7 close (gf3n Milestone M1).** New `src/extract/heap.jl` (~772 LOC): a non-mutating extraction pre-pass `_detect_gc_preamble!` recognising the dead Julia GC/heap skeleton (TLS inline-asm read, `@ijl_gc_small_alloc`, `j_#_growend!`, GC-frame chain, `Memory{T}` header stores, capacity diamond), proving it dead w.r.t. the return via a 6-part bail-loud liveness proof, and telling the module walker which LLVM instructions to skip. `mem=:heap` opt-in mode threaded `reversible_compile` → `extract_parsed_ir` → `_module_to_parsed_ir[_on_func]`; normalised to `:auto` before `lower()` (no lowering change, no new IR node). `f1(x::Int8)=let v=Int8[];push!(v,x);v[1] end` compiles green under `mem=:heap` (identity circuit, oracle over all 256 Int8). 3+1 protocol — 2 independent `Plan` architects diverged on dropping f1's dead `growend!`+diamond (A: erase from Module + `dce,simplifycfg`; B: non-mutating, reuse the `sret_writes.suppressed` skip idiom); orchestrator chose B (A leaves the module transiently malformed; CLAUDE.md §5/§10 lean against a second codegen stage). P-opcode obligation added in review (rejects tainted atomicrmw/cmpxchg/invoke/callbr/fence — `invoke`/`callbr` slip past a naive `opcode==Call` check). New `test/test_gps7_heap_m1.jl` 519/519; peer regressions green (increment 257/257, gate-count 39/39 unchanged, t5_corpus 258/258, t0_preprocessing, universal_dispatch 293/293). Gotchas: `LLVM.operands()` crashes on `GlobalAlias` operands → heap.jl uses raw C-API operand access; `Memory{T}` header data ptr is a module global (constant, not SSA) → seed-5 (global-address loads seeded structurally); f1 has THREE allocas; the `#_growend!##0#_growend!##1` substring also appears in an alloca value-name → growend regex matches callee names only. `Bennett-gf3n` epic split into 4 child beads (`gps7`/`kuza`/`5ikt`/`bd5f`); `Bennett-cc0.5` retired as superseded. Next: M2 `Bennett-kuza` (`Array{T}(undef,const_N)` synthetic alloca, 3+1). |
| [072_2026-05-21_qrom_paper_assessment.md](worklog/072_2026-05-21_qrom_paper_assessment.md) | 313 | Session log — 2026-05-21: backlog stocktake + LLVM-opcode coverage stocktake + next-agent handoff (the dynamic-memory frontier directive). Below it (newest-first): the **heap-memory 3+1 design effort** (design-only — validated consensus design + 4-milestone roadmap, epic `Bennett-gf3n`), **Bennett-8su4 close** (volatile `c=0` GC-frame memset accepted; predicate relocation in `_handle_memset_arm`) + the heap-memory de-risking spike (SROA-dissolution fear refuted; the next wall is inline-asm not a GEP; TJ4 is a store-to-load mirage → `Bennett-890r`), and the **QROM optimization paper assessment** (Motlagh-Pocrnic 2026 NOT applicable — dirty-ancilla; Low-Kliuchnikov-Schaeffer 2024 the right clean-ancilla reference but deferred — `Bennett-p4ch`). |
| [071_2026-05-18_smjd_persistent_diamond.md](worklog/071_2026-05-18_smjd_persistent_diamond.md) | 290 | Session log — 2026-05-20: **Bennett-2uas close (T5 EPIC CLOSED)** — T5-P7b docs pass: BENCHMARKS.md Pareto subsection + `docs/paper_outline_T5.md` (NEW) + README. Below it (newest-first): **Bennett-ktt8** (T5-P7a head-to-head Pareto benchmark — `:linear_scan` wins every cell), **Bennett-qi6c** (`:cf` arm — all four `persistent_impl` impls now wired), **Bennett-d746** (`:hamt` arm), **Bennett-25dm triage**, **Bennett-6883** (`:okasaki` arm), **Bennett-smjd** — non-entry-block persistent stores via block-pred-guarded MUX (Plan Option A: output-MUX). `src/lowering/memory.jl` gains `_lower_store_via_persistent_guarded!` + `_emit_persistent_set_unconditional!` (factored entry-block fast path); `_lower_store_via_persistent!` becomes a 12-line dispatcher splitting on `block_label === ctx.entry_label`. Non-entry-block stores: unconditional `IRCall` to `impl.pmap_set` → `lower_mux!(ctx.gates, ctx.wa, [pred_wire], post_state, pre_state, state_w)` → rebind `ctx.vw[alloca_dest] = merged`. No new IR opcodes, no new callees, no new BennettStrategy. `test/test_t5_p6_persistent_dispatch.jl` testsets 1+3 rewritten using hand-built `.ll` fixtures parsed via `LLVM.Context() + parse(LLVM.Module, …) + Bennett._module_to_parsed_ir` (mirroring `test_memory_corpus.jl::_compile_ir`); testset 3 flipped RED→GREEN with 4 corner cases + 8-trial random sweep + gate-count comparison vs equivalent shadow-memory diamond baseline (`alloca i8, i32 256` → `:shadow_checkpoint`). **Key learning:** the z2dj-era "callee-inlining bypasses dispatcher" hypothesis (worklog/070 gotcha 1) was wrong — the real root cause is Julia frontend SROA on NTuple state, which decomposes the persistent state into scalar SSAs before any LLVM alloca materialises; `@noinline` cannot fix this. The `.ll`-fixture pattern is the only robust way to test persistent dispatcher paths. Diamond-CFG gate counts (raw, not pinned): persistent=3718 / shadow=40502 → persistent ≈ 0.09× (much better than the 4× ceiling). All 7 regression files green: test_t5_p6_persistent_dispatch.jl 323/323; test_persistent_interface.jl 88/88 (entry-block anchor total=404 Toffoli=90 UNCHANGED); test_gate_count_regression.jl 39/39; test_kmuj_callee_groups.jl 327/327 (no callee added); test_increment.jl 257/257; test_universal_dispatch.jl 293/293; test_memory_corpus.jl 582/582; test_self_reversing.jl 12/12. Bennett-8liz closed-as-superseded. |
| [070_2026-05-16_doih_global_src_memcpy.md](worklog/070_2026-05-16_doih_global_src_memcpy.md) | ~245 | Session log — 2026-05-16: **Bennett-z2dj close** — T5-P6 `:persistent_tree` dispatcher arm. Full 13-step `docs/design/p6_consensus.md` §5 plan; 11 of 12 steps shipped this session (Step 12 = bd-close + push), orchestrator dispatched opus implementers serially. Below: **Bennett-doih close** — global-pointer src memcpy. Splits `_handle_memcpy_arm` predicate 5 into 5a (DST-as-global hard reject) and 5b (SRC-as-global → new `_handle_memcpy_global_src` arm). Threads `parsed.globals` dict `_module_to_parsed_ir_on_func` → `_convert_instruction` (new kwarg) → `_handle_intrinsic` → `_handle_memcpy_arm` → new arm. G1-G9 cascade emits K element-granular `IRPtrOffset + IRStore(iconst, dst_ew)` pairs pulling per-element constants from `parsed.globals[gname].data` (mirror of Bennett-9nwt case C). 77/77 in new `test_doih_memcpy_global_src.jl` (12 testsets, 5 positive + 7 reject fixtures). All 8 peer regressions green (37mt 86/86, 9nwt 87/87, ixiz 53/53, munq 69/69, lqif 12/12, q04a 9/9, 8kno 8/8, gate-count 39/39); zero pre-existing reject tests flipped. **Bead acceptance fixture (t5_tr2_hashmap.ll:153) unreachable by MVP** — both proposers caught that the referenced global is `<{ ptr, [24 x i8] }>` ConstantStruct, not ConstantDataArray, and `_extract_const_globals` silently skips it; honest scope tightening per Bennett-ixiz lesson, file `Bennett-zxhg` follow-up for ConstantStruct extension. 3 follow-up beads filed: `Bennett-zxhg` (P3, struct globals), `Bennett-epfe` (P4, cross-width byte-packing), `Bennett-ui4f` (P4, variable-GEP true QROM). q04a contract preserved via kwarg-with-default threading (no Tuple update needed). |
| [069_2026-05-16_rjk7_self_reversing_all_strategies.md](worklog/069_2026-05-16_rjk7_self_reversing_all_strategies.md) | ~480 | Session log — 2026-05-16 (top): **Bennett-jpa5 close as won't-fix** — auto self_reversing non-slicing qcla_tree variant. Triage confirmed REWORK annotation: LLVM `mul` always has matching operand/result widths and `_iwidth(inst)` reads result type, so the dead-code branch at `arith.jl:230` (`if length(full) == W`) is structurally unreachable. Option (c) ("detect zext-provenance + fire producer-tag") rejected because the annotation missed a SECOND blocker: `_infer_self_reversing` (bennett_transform.jl:176, condition 4) rejects ANY LR whose non-tagged groups aren't `__pred_*` / `__ret_*` / `__branch_*` — a zext-fed widening mul has IRCast groups (`__v1`, `__v2`) that hold real data and are NOT in the boilerplate allowlist, so firing the producer-tag would be a no-op end-to-end. Real fix needs CORE 3+1 work to extend `_infer_self_reversing` to allowlist IRCast groups (or fuse cast+mul) — out of scope for jpa5's P3 framing. Drive-by: comment block at arith.jl:218-247 rewritten to name the structural blockers honestly instead of hand-wavy "future work (h0ai-followup-D)". Code-behavior unchanged. Bigger picture covered by `Bennett-pzft` (broader "uncompute high-W qcla_tree wires" attack on the same root problem). Below: **Bennett-ixiz close** — wider-element alloca support across extract+lowering pipeline. 5 gate sites lifted (G1 aggregate.jl:227, G3/G4/G5 in extract/instructions.jl, plus implementer-caught 4th duplicate at _convert_instruction:2032). **Bead description was wrong about scope** — said "two gates including memory.jl:245-246" but memory.jl Gate-2 sites already check `inst.width == elem_w` (not `== 8`) and serve as mixed-width firewall. Both proposers + implementer caught extract-layer gates the bead missed. 53/53 in new `test_ixiz_wider_alloca.jl`; 4 reject-test flips (test_munq/test_37mt/test_9nwt/test_lqif, last was implementer drive-by); 1322 asserts green across alloca/memcpy/memset peer regressions. 2 sibling beads filed (Bennett-2fue P3 mixed-width support, Bennett-a5ag P4 (N=1,W>=64) dispatcher gap). Below: **Bennett-rjk7 close** (universal `lr.self_reversing` fast-path across all 6 Bennett strategies via inlined `_validate_self_reversing!` + `_build_circuit` pattern; mirrors `_bennett_default` exactly). **Critical contract gap finding**: pre-rjk7 `EagerStrategy` silently accepted forged tags; other 4 strategies threw but via inconsistent algorithm-internal checks (not U03); post-rjk7 all 6 throw `ArgumentError` uniformly via the U03 probe per §1. 3+1 protocol (helper-vs-inline call decided on readability under debugging; central-only docstring; parametrized-loop tests with named inner @testset for bisect). 3098/3098 in new `test_rjk7_self_reversing_all_strategies.jl` (3 groups × 6 strategies = 18 testsets). Side cleanups same session: `Bennett-2ebq` closed as superseded by `lx5h`; `Bennett-jpa5` annotated with rework finding (premise not expressible by current extractor). |
| [068_2026-05-15_h0ai_auto_self_reversing.md](worklog/068_2026-05-15_h0ai_auto_self_reversing.md) | ~360 | Session log — 2026-05-15 (top): **Bennett-hzl9 close** (h0ai follow-up A — producer-tag for `lower_tabulate`). Emits `:__tabulate_qrom` GateGroup over QROM block + threads `auto_self_reversing` kwarg into both `reversible_compile` tabulate call sites — kill switch was silently ignored pre-hzl9, now honored end-to-end. +261 asserts (132,167 → 132,428 in `test_h0ai_auto_self_reversing.jl`). Below: **Bennett-h0ai close** (the auto self_reversing detection infrastructure itself — 3-layer: producer-tag in `GateGroup.is_self_reversing` + structural aggregator `_infer_self_reversing` + runtime probe `_validate_self_reversing!` with `trusted_dirty_wires` allowlist). 3+1 protocol observed. Was "conservative-only today" at h0ai close (no real-world function triggered auto-promotion); hzl9 made QROM the first real producer to fire. |
| [067_2026-05-15_pg5_vector_reductions.md](worklog/067_2026-05-15_pg5_vector_reductions.md) | ~285 | Session log — 2026-05-15 (top): **Bennett-lx5h close** — native dispatch for the 8 LLVM float vector-reduction intrinsics (`llvm.vector.reduce.{fadd,fmul,fmin,fmax,fminimum,fmaximum,fminimumnum,fmaximumnum}.*`); extends `_handle_vector_reduction` in `src/extract/vectors.jl` with `(:fcall, soft_fn)` dispatch kind folding lanes via `IRCall` over `soft_fadd`/`soft_fmul`/`soft_fmin`/`soft_fmax`/`soft_fminimum`/`soft_fmaximum`/`soft_minimumnum`/`soft_maximumnum`. Structural wrinkle: fadd/fmul scalar START arg → 3-operand layout, `start` as initial accumulator, fold ALL N lanes. Strict left-to-right per §13 — `reassoc` IGNORED. 71/71 across 10 testsets (10 fixtures: 8 intrinsics × v4f64 + v2f64 fadd corner + 2 f32 reject). Below: **Bennett-pg5 close** — 9 LLVM integer vector-reduction intrinsics via new `_handle_vector_reduction` helper; left-to-right linear fold chain. 56/56 (post-lx5h update preserves count). Both 3+1 skipped per mechanical-extension exception. |
| [066_2026-05-15_kh6n_prefix_discipline.md](worklog/066_2026-05-15_kh6n_prefix_discipline.md) | ~660 | Session log — 2026-05-15 (top, p19b then mq6f then k2w6 then kh6n): **Bennett-p19b close — kh6n FUTURE-WORK PIPELINE NOW CLOSED.** `soft_minimumnum` / `soft_maximumnum` thin `function … end` aliases over `soft_fmin` / `soft_fmax` (which already chose the IEEE 754-2019-spec'd ±0 tie-break). LLVM 19+ `llvm.minimumnum.f64` / `llvm.maximumnum.f64` native dispatch replaces the third (and last) kh6n explicit-reject. `_CALLEES_FP_MINMAX` 4 → 6, soft-float primitives 37 → 39. 227,276 asserts green incl. alias-identity invariant, IEEE 754-2019 reference matrix, 1074-binade subnormal sweep × ± × 4 partners × 2 primitives. **Bennett-mq6f close** (`soft_round_away` round-half-AWAY + `llvm.round.` native dispatch — closes a SILENT MISCOMPILE where pre-mq6f raw `.ll` ingest of `llvm.round(2.5)` returned `2.0` (banker's) instead of `3.0` (away); also corrects kh6n misstatement that `soft_round` was round-half-AWAY — `soft_round` IS banker's per Bennett-2hhx; `llvm.roundeven.` reject replaced with native dispatch to `soft_round`; 7367 asserts) + **Bennett-k2w6 close** (`soft_fmin`/`soft_fmax` NaN-absorbing + `soft_fminimum`/`soft_fmaximum` NaN-propagating; LLVM `llvm.minnum`/`llvm.maxnum`/`llvm.minimum`/`llvm.maximum` dispatch + `Base.min`/`Base.max` SoftFloat overrides; 434,472 asserts incl. 100k random pairs × 4 + 1074-binade subnormal sweep × 4; closes kh6n future-work stub) + **Bennett-kh6n close** (trailing-`.` prefix-discipline codemod for 32 sites in `src/extract/instructions.jl` + drive-by sret.jl, plus explicit fail-loud rejects for `llvm.minimumnum`/`maximumnum`/`roundeven` sibling intrinsics — ALL THREE rejects now SUPERSEDED by p19b + mq6f; 13/13 new + 6981 regression asserts). |
| [065_2026-05-14_k31q_check_bounds.md](worklog/065_2026-05-14_k31q_check_bounds.md) | ~330 | Session log — 2026-05-15 (top) — **Bennett-8ygo + Bennett-bipw close**: `soft_atanh` and `soft_acosh` log1p refactors (drop K=25 and K=15 polynomials respectively, use Julia stdlib natural forms). Then **Bennett-tfmo close**: `soft_asinh` four-regime Julia stdlib log1p refactor (drops K=30 polynomial, 0 ULP spot-checks, 5178/5178 + 31/31). Then **Bennett-9wmk attempt+revert**: bare mid-algorithm fast_copy swap saves ~25% wire but adds 30% depth — bead left OPEN. Then **Bennett-gsxe close**: Cuccaro 2W−2 → 2W−3 via §3.5. Then 2026-05-14 **Bennett-k31q close**. |
| [064_2026-05-13_project_orientation_beads.md](worklog/064_2026-05-13_project_orientation_beads.md) | ~283 | Session log — 2026-05-13/14 — orientation session; then 2026-05-14: ao66 follow-up tidy (trailing-`.`, 3+1 record correction), `runtests.jl` `runfile` progress markers + `BENNETT_HEAVY_TESTS` gate, `test_kmuj_callee_groups.jl` stale-count fix. |
| [063_2026-05-06_g82n_soft_atanh_TIER_C1_COMPLETE.md](worklog/063_2026-05-06_g82n_soft_atanh_TIER_C1_COMPLETE.md) | ~340 | Session log — 2026-05-06 — **Bennett-g82n close — TIER C1 COMPLETE 11 of 11**. Tier C1.11 — `soft_atanh` + dispatch for `llvm.atanh.f64` and libm `@atanh`. Three-regime branchless port: `|x|>1→NaN` / K=25 polynomial in z=x² for `|x|≤0.5` (exact rationals `c_k=1/(2k+1)`) / `0.5·log((1+|x|)/(1-|x|))` for medium. ODD function, ±1→±Inf via natural log propagation. _CALLEES_FP_TRANS 24→25. |
| [062_2026-05-06_sfx9_soft_asinh.md](worklog/062_2026-05-06_sfx9_soft_asinh.md) | ~250 | Session log — 2026-05-06 — TWO concatenated entries (newest first): **Bennett-eq9p close** (Tier C1.10 — `soft_acosh` + dispatch for `llvm.acosh.f64` and libm `@acosh`; fifth hyperbolic, first domain-restricted; four-regime branchless port with NaN for x<1; K=15 polynomial via `s²=2(x-1)` substitution for x ∈ [1, 1.3]; _CALLEES_FP_TRANS 23→24) + **Bennett-sfx9 close** (Tier C1.9 — `soft_asinh` + dispatch for `llvm.asinh.f64` and libm `@asinh`; fourth hyperbolic; K=30 polynomial in z=x² for |x|≤0.55; _CALLEES_FP_TRANS 22→23). |
| [061_2026-05-06_ky5n_soft_sinh.md](worklog/061_2026-05-06_ky5n_soft_sinh.md) | ~265 | Session log — 2026-05-06 — TWO concatenated entries (newest first): **Bennett-bybh close** (Tier C1.8 — `soft_cosh` + dispatch for both `llvm.cosh.f64` and libm `@cosh`; third hyperbolic; mirror of ky5n with even-function simplifications; §13 contract `cosh(subnormal) === 1.0`; 3+1 protocol skipped per §2 exception; _CALLEES_FP_TRANS 21 → 22) + **Bennett-ky5n close** (Tier C1.7 — `soft_sinh` + dispatch for both `llvm.sinh.f64` and libm `@sinh`; second hyperbolic; three-regime branchless port; `soft_exp_fast` NaN bug at ~709.78 worked around with conservative 709.0 threshold; _CALLEES_FP_TRANS 20 → 21). |
| [060_2026-05-06_m2bv_soft_tanh.md](worklog/060_2026-05-06_m2bv_soft_tanh.md) | ~160 | Session log — 2026-05-06 — **Bennett-m2bv close** (Tier C1.6 — `soft_tanh` + dispatch for both `llvm.tanh.f64` and libm `@tanh`; first hyperbolic primitive). Branchless port of Julia stdlib `Base.tanh` (degree-10 polynomial `Base.tanh_kernel` for `|x| ≤ 0.5` / exp-formula for medium / saturate for `|x| ≥ 22`). ONE `soft_exp_fast` call (FTZ branch unreachable). ≤2 ULP across 100k random × 3 seeds; **§13 subnormal-INPUT bit-exact (0 ULP across all 1074 binades × both signs) — implicit via the polynomial branch's algebra**, no override needed. _CALLEES_FP_TRANS 19 → 20. |
| [059_2026-05-06_7goc_soft_atan2.md](worklog/059_2026-05-06_7goc_soft_atan2.md) | ~210 | Session log — 2026-05-06 — **Bennett-7goc close** (Tier C1.5 — `soft_atan2` + dispatch for both `llvm.atan2.f64` and libm `@atan2`; built on `soft_atan` per §12; ≤2 ULP on 100k random × 3 seeds × 4 quadrants; drive-by fix tightened all six trig prefixes with trailing `.` to prevent silent miscompile that matched `llvm.atan2.f64` against the `llvm.atan` arm pre-7goc). _CALLEES_FP_TRANS 18 → 19. |
| [058_2026-05-04_ckvj_soft_asin.md](worklog/058_2026-05-04_ckvj_soft_asin.md) | ~340 | Session log — 2026-05-04 / 05 — two entries top-down: **Bennett-bd7f close** (Tier C1.4 — `soft_acos` + `llvm.acos` dispatch; reuses `_asin_R` from fasin.jl per §12; ≤2 ULP on 100k random × 3 seeds; subnormal-output proven absent per §13) + **Bennett-ckvj close** (Tier C1.3 — `soft_asin` + `llvm.asin` dispatch; faithful musl `e_asin.c` port; ≤2 ULP on 100k random × 3 seeds; 1075-input subnormal binade at 0 ULP). _CALLEES_FP_TRANS extended 16 → 17 → 18. |
| [057_2026-05-04_s1zl_soft_tan.md](worklog/057_2026-05-04_s1zl_soft_tan.md) | ~240 | Session log — 2026-05-04 — two entries top-down: **Bennett-qpke close** (Tier C1.2 — `soft_atan` + `llvm.atan` dispatch; max ULP ≤ 2 on 500k random) + **Bennett-s1zl close** (Tier C1.1 — `soft_tan` + `llvm.tan` dispatch; max ULP = 1 on 500k random). _CALLEES_FP_TRANS extended 14 → 15 → 16. |
| [056_2026-05-03_37mt_memcpy_phase1.md](worklog/056_2026-05-03_37mt_memcpy_phase1.md) | ~430 | Session log — 2026-05-03 evening — three concatenated entries (newest first): **Bennett-8bys research/scope-split + Bennett-munq close** (8bys split into 6 sub-beads; munq landed `[N x i8]` ArrayType extraction, 61 asserts), **Bennett-9nwt close** (Phase 2 memset, 82 asserts), **Bennett-37mt close** (Phase 1 memcpy, 80 asserts). Total Bennett-hao progress: lqif/37mt/9nwt/munq closed; ixiz/xtu9/yxr8/zmry/doih open under 8bys umbrella. |
| [055_2026-05-03_3mo_sin_cos.md](worklog/055_2026-05-03_3mo_sin_cos.md) | ~210 | Session log — 2026-05-03 — **Bennett-3mo close**: `soft_sin` / `soft_cos` (musl + full Payne-Hanek). 100k random sweep cos=0 ULP, sin=1 ULP, 0 fails > 2 ULP. Five gotchas captured incl. UInt128-constant rejection. |
| [054_2026-05-02_soft_log.md](worklog/054_2026-05-02_soft_log.md) | ~370 | Session log — 2026-05-02 — top: **Bennett-jexo close** (`soft_pow_julia` Path A: bit-exact vs `Base.:^`; `Base.:^(::SoftFloat,::SoftFloat)` routes to it; 300k/300k random samples bit-exact; muladd↔mul+add gotcha at `@noinline` sites). Below it: **Bennett-582 close** (log family) + **Bennett-emv close** (`soft_pow` / `soft_powi` musl + `llvm.pow` / `llvm.powi` dispatch). |
| [053_2026-05-01_dnh_research_nj6c.md](worklog/053_2026-05-01_dnh_research_nj6c.md) | ~265 | Session log — 2026-05-01 (late evening) — **Bennett-cb9y + Bennett-dnh close → OPCODE PARITY for runtime-indexed memory** (multi-origin × runtime-idx walls closed; dnh itself closes), **Bennett-nj6c close** (dnh phase 1a — `_MUX_SHAPES_NW` extended to all (N,W) with N·W ≤ 64), **Bennett-dnh research/re-scope** (six-agent research sweep). |
| [052_2026-05-01_i2ca_strategy_dispatch.md](worklog/052_2026-05-01_i2ca_strategy_dispatch.md) | ~250 | Session log — 2026-05-01 (evening) — top: **Bennett-kv7b / U65 close** (test-coverage epic; 8 final sub-items batched: test_two_args 256→exhaustive, persistent-map bounds, dep_dag semantics, memssa end-to-end, add×mul cross, controlled × soft-float). Below: **Bennett-i2ca / U55 close** (`bennett(lr; strategy=...)` dispatch via `abstract type BennettStrategy` + 6 subtypes; 5 legacy aliases as forwarders; helpers extracted; 3+1 protocol). |
| [051_2026-05-01_eleven_close_grind.md](worklog/051_2026-05-01_eleven_close_grind.md) | ~559 | Session log — 2026-05-01 (afternoon) — **11 catalogue closes**: iwv5/u71l/x2iw/u2yp/vpch/3rph/gk1h/fidj/58rl/sa39/8403. Catalogue 98 % closed at session end. |
| [050_2026-05-01_v958_ehoa_lm3x.md](worklog/050_2026-05-01_v958_ehoa_lm3x.md) | ~439 | Session log — 2026-05-01 (morning) — 4 catalogue closes (s92x U115 SretInfo struct; lm3x U56 MUX consolidation; ehoa U43 LoweringCtx concretization; v958 U68 IROperand abstract hierarchy via 3+1). All 419k tests pass. |
| [049_2026-04-30_vdlg_x3jc_zpj7_kv7b.md](worklog/049_2026-04-30_vdlg_x3jc_zpj7_kv7b.md) | ~150 | Session log — 2026-04-30 (continuation 2) — kv7b grind 3 more sub-items (SHA-256 + mul dispatcher + vector_ir → exhaustive), preceded by x3jc + zpj7 + kv7b loop-widths, preceded by vdlg lower.jl 9-file split. Three structural splits + four kv7b sub-items in one day. |
| [048_2026-04-27_1xub_persistent_ds_refresh.md](worklog/048_2026-04-27_1xub_persistent_ds_refresh.md) | ~235 | Session log — 2026-04-28 — LOC-tier grind (51 beads closed + kv7b epic partials); below it: 2026-04-28 qxg9 close detail, 2026-04-27 qxg9 bisect data, 1xub close. |
| [047_2026-04-27_heup_fold_constants_doc_only.md](worklog/047_2026-04-27_heup_fold_constants_doc_only.md) | ~380 | Session log — 2026-04-27 (3+1 refactor, stage 1) — Bennett-tzrs partial (extract _handle_intrinsic) — followed by 6 more 2026-04-27 sessions: tbm6 / 7xng / cvnb / jc0y / q04a / heup |
| [046_2026-04-27_y986_loop_header_dispatch.md](worklog/046_2026-04-27_y986_loop_header_dispatch.md) | ~510 | Session log — 2026-04-27 — Bennett-y986 / U05-followup-2 close (loop-header dispatch unification) |
| [045_2026-04-27_day_summary_bugs_only_directive.md](worklog/045_2026-04-27_day_summary_bugs_only_directive.md) | 283 | Session log — 2026-04-27 — Bennett-salb / U119 close (div-by-0 + signed typemin/-1 contract) |
| [044_2026-04-26_late_night_doh6.md](worklog/044_2026-04-26_late_night_doh6.md) | 282 | Session log — 2026-04-27 (early morning) — Bennett-3of2 / U112 close (wire-leak investigated, doc-only) + Bennett-vt0a filed |
| [043_2026-04-26_late_afternoon_wlf6_6u9q.md](worklog/043_2026-04-26_late_afternoon_wlf6_6u9q.md) | 274 | Session log — 2026-04-26 (late night) — 9c4o close (lower.jl deps load before lower.jl) |
| [042_2026-04-26_midday_d1ee_f6qa.md](worklog/042_2026-04-26_midday_d1ee_f6qa.md) | 270 | Session log — 2026-04-26 (afternoon) — hjbf + 8p0g closes (Contributing section + ParsedIR seam test) |
| [041_2026-04-26_morning_069e_k7al.md](worklog/041_2026-04-26_morning_069e_k7al.md) | 270 | Session log — 2026-04-26 (late-morning) — 8kno + zy4u closes (LLVM.initializer narrowing + outer @testset) |
| [040_2026-04-25_late_night_348q_tfo8.md](worklog/040_2026-04-25_late_night_348q_tfo8.md) | 280 | Session log — 2026-04-26 (early morning) — uzic + uinn closes (citations + InterruptException narrowing) |
| [039_2026-04-25_evening_softfloat_grind.md](worklog/039_2026-04-25_evening_softfloat_grind.md) | 250 | Session log — 2026-04-25 (post-night) — 6t8s + ej4n closes (mechanical rename + callee ParsedIR cache) |
| [038_2026-04-25_uoem_persistent_ds_research_relocation.md](worklog/038_2026-04-25_uoem_persistent_ds_research_relocation.md) | 269 | Session log — 2026-04-25 (afternoon) — catalogue grind, 13 P2 beads cleared |
| [037_2026-04-25_preamble.md](worklog/037_2026-04-25_preamble.md) | 280 | Session log — 2026-04-25 — Bennett-jppi deferred, Bennett-p1h1 README Project-status refresh |
| [036_2026-04-24_…_evening_session_close.md](worklog/036_2026-04-24_next_agent_start_here_evening_session_close.md) | 315 | NEXT AGENT — start here — 2026-04-24 (evening session close) |
| [035_2026-04-24_session.md](worklog/035_2026-04-24_session.md) | 259 | Session — 2026-04-24 |
| [034_2026-04-22_…_catalogue_beads_phase.md](worklog/034_2026-04-22_next_agent_previous_context_catalogue_beads_phase.md) | 579 | (continuation of 033 — single monolithic narrative; one of the few outliers > 300 lines that resists clean splitting) |
| [033_2026-04-22_…_catalogue_beads_phase.md](worklog/033_2026-04-22_next_agent_previous_context_catalogue_beads_phase.md) | 196 | NEXT AGENT — start here — 2026-04-21 (mother-of-all code review landed) |
| [032_2026-04-21_…_t5_p6_unblocked.md](worklog/032_2026-04-21_next_agent_shipped_t5_p6_unblocked_earlier_in_day.md) | 151 | NEXT AGENT — 2026-04-21 (α+β+γ shipped; T5-P6 unblocked) — earlier in day |
| [031_2026-04-21_…_sret_callee_infrastructure.md](worklog/031_2026-04-21_session_log_sret_callee_infrastructure.md) | 303 | Session log — 2026-04-21 — α + β + γ (sret + callee infrastructure) |
| [030_2026-04-21_…_t5_p5_multi_language.md](worklog/030_2026-04-21_session_log_bennett_lmkb_bennett_f2p9_closed_t5_p5.md) | 196 | Session log — 2026-04-21 — Bennett-lmkb + Bennett-f2p9 (T5-P5a + P5b multi-language ingest) |
| [029_2026-04-21_…_cc0_4_constantexpr_icmp.md](worklog/029_2026-04-21_session_log_bennett_cc0_4_closed_constantexpr_icmp.md) | 268 | Session log — 2026-04-21 — Bennett-cc0.4 closed (ConstantExpr icmp folding) |
| [028_2026-04-20_…_cc0_7_slp_vectorised.md](worklog/028_2026-04-20_session_log_bennett_cc0_7_closed_slp_vectorised_ir.md) | 134 | Session log — 2026-04-20 — Bennett-cc0.7 closed (SLP-vectorised IR support) |
| [027_2026-04-16_…_m1_m2_m3_landed.md](worklog/027_2026-04-16_next_agent_start_here_m1_m2a_d_m3a_landed_m3b_or_m.md) | 263 | NEXT AGENT — start here — 2026-04-16 (M1 + M2a-d + M3a landed) |
| [026_2026-04-20_…_persistent_ds_scaling_sweep.md](worklog/026_2026-04-20_session_log_persistent_ds_scaling_sweep_phase_3_co.md) | 225 | Session log — 2026-04-20 — Persistent-DS scaling sweep (Phase-3 reversed at scale) |
| [025_2026-04-17_…_t5_phase_3_complete.md](worklog/025_2026-04-17_session_log_t5_phase_3_complete_p3a_interface_p3b.md) | 241 | Session log — 2026-04-17 — T5 Phase 3 complete (P3a + P3b/c/d 3 persistent-DS impls) |
| [024_2026-04-16_…_m3a_shadow_checkpoint.md](worklog/024_2026-04-16_session_log_m3a_bucket_a_b_spillover_t4_shadow_che.md) | 167 | Session log — 2026-04-16 — M3a Bucket A/B-spillover T4 shadow-checkpoint MVP (Bennett-jqyt) |
| [023_2026-04-16_…_m2d_mux_store_gating.md](worklog/023_2026-04-16_session_log_m2d_bucket_c3_mux_store_gating_bennett.md) | 223 | Session log — 2026-04-16 — M2d Bucket C3 MUX-store gating (Bennett-i2a6) |
| [022_2026-04-16_…_m2b_pointer_typed_phi.md](worklog/022_2026-04-16_session_log_m2b_bucket_c2_pointer_typed_phi_select.md) | 273 | Session log — 2026-04-16 — M2b Bucket C2 pointer-typed phi/select (Bennett-tzb7) |
| [021_2026-04-16_…_m1_parametric_mux_exch.md](worklog/021_2026-04-16_session_log_m1_bucket_a_parametric_mux_exch_green.md) | 210 | Session log — 2026-04-16 — M1 Bucket A (parametric MUX EXCH) GREEN |
| [020_2026-04-16_…_m2a_cross_block_ptr.md](worklog/020_2026-04-16_session_log_m2a_bucket_c1_cross_block_ptr_provenan.md) | 318 | Session log — 2026-04-16 — M2a Bucket C1 cross-block ptr_provenance |
| [019_2026-04-16_…_soft_fma.md](worklog/019_2026-04-16_session_log_soft_fma_ieee_754_binary64_fma_bennett.md) | 263 | Session log — 2026-04-16 — soft_fma (IEEE 754 binary64 FMA) (Bennett-0xx3) |
| [018_2026-04-16_…_soft_exp_subnormal_fix.md](worklog/018_2026-04-16_session_log_soft_exp_soft_exp2_bit_exact_subnormal.md) | 167 | Session log — 2026-04-16 — soft_exp / soft_exp2 bit-exact subnormal output via musl specialcase |
| [017_2026-04-16_…_soft_exp_base_exp.md](worklog/017_2026-04-16_session_log_soft_exp_soft_exp2_base_exp_softfloat.md) | 292 | Session log — 2026-04-16 — soft_exp / soft_exp2 + Base.exp(::SoftFloat) (Bennett-cel) |
| [016_2026-04-15_…_soft_fsqrt_base_sqrt.md](worklog/016_2026-04-15_session_log_soft_fsqrt_base_sqrt_softfloat_bennett.md) | 316 | Session log — 2026-04-15 — soft_fsqrt + Base.sqrt(::SoftFloat) (Bennett-ux2) |
| [015_2026-04-14_…_workstream_complete.md](worklog/015_2026-04-14_session_log_continuation_workstream_complete.md) | 264 | Session log — 2026-04-14 (continuation) — advanced-arithmetic workstream complete |
| [014_2026-04-13_…_bc_3_full_sha256_sret.md](worklog/014_2026-04-13_session_log_bc_3_full_sha_256_sret_support.md) | 319 | Session log — 2026-04-13 — BC.3 full SHA-256 + sret support |
| [013_2026-04-09_session_log.md](worklog/013_2026-04-09_session_log.md) | 210 | Session log — 2026-04-09 |
| [012_2026-04-09_…_v0_5_float64.md](worklog/012_2026-04-09_session_log_continued_v0_5_float64.md) | 140 | Session log — 2026-04-09 (continued): v0.5 Float64 |
| [011_…_research_overflow_false_path.md](worklog/011_9999-99-99_research_overflow_simulation_bug_false_path_proble.md) | 220 | Research: overflow simulation bug — "false path" problem (literature survey + reference, no date) |
| [010_2026-04-10_…_branchless_soft_fadd.md](worklog/010_2026-04-10_session_log_branchless_soft_fadd_option_a.md) | 317 | Session log — 2026-04-10: branchless soft_fadd (Option A) |
| [009_2026-04-11_…_documentation_narrow_bit_width.md](worklog/009_2026-04-11_session_log_continued_documentation_narrow_bit_wid.md) | 242 | Session log — 2026-04-11 (continued): Documentation + narrow bit-width |
| [008_2026-04-10_…_switch_reversible_memory_research.md](worklog/008_2026-04-10_switch_instruction_reversible_memory_research_benn.md) | 263 | 2026-04-10 — Switch instruction + reversible memory research (Bennett-282) |
| [007_2026-04-10_…_float64_division_multi_arg.md](worklog/007_2026-04-10_fix_float64_division_and_multi_arg_float64_compile.md) | 252 | 2026-04-10 — Fix Float64 division and multi-arg Float64 compile (Bennett-dqc) |
| [006_2026-04-11_…_pebbling_pipeline_eager.md](worklog/006_2026-04-11_pebbling_pipeline_gate_groups_value_level_eager_be.md) | 313 | 2026-04-11 — Pebbling pipeline: gate groups + value-level EAGER (Bennett-an5) |
| [005_2026-04-11_…_checkpoint_bennett_sha256.md](worklog/005_2026-04-11_checkpoint_bennett_66_wire_reduction_on_sha_256_be.md) | 320 | 2026-04-11 — Checkpoint Bennett: 66% wire reduction on SHA-256 (Bennett-an5) |
| [004_2026-04-12_…_irstore_iralloca_types.md](worklog/004_2026-04-12_memory_plan_t1a_1_irstore_iralloca_types_bennett_f.md) | 309 | 2026-04-12 — Memory plan T1a.1: IRStore / IRAlloca types (Bennett-fvh) |
| [003_2026-04-12_…_handoff_session_close.md](worklog/003_2026-04-12_handoff_for_next_agent_session_close.md) | 287 | HANDOFF FOR NEXT AGENT — 2026-04-12 session close |
| [002_2026-04-12_…_qrom_vs_mux_scaling.md](worklog/002_2026-04-12_t1c_3_qrom_vs_mux_scaling_benchmark_bennett_qw8k.md) | 232 | 2026-04-12 — T1c.3: QROM vs MUX scaling benchmark (Bennett-qw8k) |
| [001_2026-04-12_…_4_round_feistel.md](worklog/001_2026-04-12_t3a_1_4_round_feistel_reversible_hash_bennett_bdni.md) | 288 | 2026-04-12 — T3a.1: 4-round Feistel reversible hash (Bennett-bdni) |
| [000_2026-04-12_…_session_close_memory_plan_push.md](worklog/000_2026-04-12_session_close_second_of_day_memory_plan_push.md) | 205 | Session close — 2026-04-12 (second-of-day, memory plan push) |

## Notes on the sharded layout

- **Why the file numbering doesn't match strict date order**: the original
  WORKLOG was *mostly* reverse-chronological but sessions were sometimes
  appended at the bottom rather than prepended at the top, so e.g. some
  2026-04-12 content sits below 2026-04-09 content in the source. The
  chunker preserves source order — the `NNN_` prefix reflects the source
  file's structure, not strict date order. The dates in filenames give the
  approximate timeframe.

- **Single outlier at 579 lines** (`worklog/034_…`): a coherent narrative
  with no internal `### ` subsections to split on. Accepted as-is rather
  than fragmenting.

- **A few chunks under 200 lines** (134-196 each): natural session-log units
  too small to merit their own chunk under strict 200-300 sizing, but
  combining them with adjacent dates would mix unrelated topics. Left as-is.

- **Reference content** (Project purpose / Repository layout / Version
  history / Key design decisions / Sturm.jl integration path) is preserved
  inside `worklog/013_2026-04-09_session_log.md` and adjacent chunks where
  it originally lived. Future cleanup may extract this to `docs/`; for now
  the byte-for-byte preservation is the priority.
