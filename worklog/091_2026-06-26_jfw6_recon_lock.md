# Worklog chunk 091

## Session log — 2026-06-29 — Bennett-u2kk: param-cell dst on global-src memcpy arm (closed-world fdict CW-D path)

**Context.** Orchestrated "next most consequential bead" session, continuing the P0
closed-world fdict critical path (cross-repo BennettVM EPIC `bennettvm-416r.11` / `7xa`)
immediately after `Bennett-xrd6` cleared the sret-call wall. With xrd6 landed,
`extract_parsed_ir_set_from_julia(fd; ptr_cells=true)` advanced to the `rehash!(Dict{Int8,Int8},Int64)`
callee body.

**The wall.** `extract_parsed_ir(Base.rehash!, Tuple{Dict{Int8,Int8},Int64}; optimize=false, ptr_cells=true)`
failed at `_handle_memcpy_global_src` G3 (`src/extract/instructions.jl` ~802):
`call void @llvm.memcpy.p0.p0.i64(ptr %"h::Dict.idxfloor_ptr", ptr @"_j_const#1", i64 8, ...)` —
the dst is a const-offset GEP off the Dict POINTER PARAMETER cell `h` (idxfloor field,
byte 48). G3 (alloca/arena-only, per Bennett-vbv9) rejected it. Four such param-field
memcpys exist in `rehash!` (idxfloor=1, ndel×2=0, maxprobe=0; all i64/8-byte;
`@_j_const#N` globals captured as `([v],64)`).

**Gotcha — ground truth from setindex!.** The GEP `%"h::Dict.idxfloor_ptr"` is ALREADY a
named `IRPtrOffset` off the Dict cell — the PROVEN `setindex!` extraction emits
`IRPtrOffset(:"h::Dict.age_ptr", SSAOperand(:"h::Dict"), 40, 8)` etc. for its field
stores via the probe-field GEP path. The fix is therefore concentrated ENTIRELY in the G3
guard; emission at ~935 works the instant G3 admits the dst.

**Process (CLAUDE.md Rule 2, 3+1 — core change to `instructions.jl`).** Claimed
`Bennett-u2kk` (P2). Two independent opus proposers CONVERGED on the same design (high
confidence). Adjudicated, adopting Proposer B's refinements: stricter resolver predicate
(`LLVMIsAArgument && PointerType` — an integer arg is never a cell root); finding that ALL
4 param-field memcpys clear with one fix; and the conservative synth-ptr fail-loud.

**Done (`src/extract/instructions.jl`).** New `_param_ptr_root_ref` (mirrors
`_gc_alloc_root_ref` / `_alloca_root_ref` const-GEP recursion; bottoms out at a
pointer-typed Argument). A THIRD G3 dst-root branch `param_root`, gated under
`ptr_cells` (consulted only when alloca+arena roots miss). `is_param` ⇒ `dst_ew=64`
(G4); G6 KEPT (cross-width fail-loud fence); G9 SKIPPED (freshness is a circuit-model
artifact — not applicable here; see below); conservative synth-ptr FAIL-LOUD. Emission
unchanged. No `ir_types.jl` change.

**Reversibility justification (the crux, documented in code).** The param-field write is
a DESTRUCTIVE OVERWRITE of a LIVE field (the caller's Dict already holds idxfloor) — NOT
fresh; vbv9's STEP-0c freshness argument does NOT apply. It is reversible because under
`ptr_cells` every cell store is logged on BVM's Bennett-1973 history tape (cell,
pre-image) and undone on reversal. Freshness (G9) is a circuit-model artifact; hence G9
is skipped here AND the `ptr_cells=false` circuit path KEEPS failing loud at G3. The
memcpy lowers to the IDENTICAL `IRPtrOffset+IRStore` shape the proven plain `setindex!`
field-store path already produces — ZERO new reversibility surface.

**Test (`test/test_u2kk_param_memcpy.jl`, RED-first).** 15 structural-invariant tests:
synthetic `.ll` fixture, cross-width G6 reject, `ptr_cells=false` byte-identity, and the
real `rehash!` wall-advance. Structural-only per Rule 5. Registered in `runtests.jl`.

**Verified (orchestrator, independent re-run).** `test_u2kk` 15/15; gate-count regression
39/39 IDENTICAL (circuit path byte-identical); `test_vbv9` 19/19 (arena byte-identical);
`test_doih` 85/85; `test_qmv7` 34/34; `test_land` 65/65 (synth-tag now `is_param`-guarded);
`test_xrd6` 16/16. **Producer advanced**: `rehash!` now extracts past ALL 4 param-field
memcpys and lands on a new, different-subsystem wall.

**Gate caught a missed regression (honest record).** The targeted regression subset OMITTED
`test_lf14_ptr_return_cells.jl` — the `setindex!`/`rehash!` landing pin, and the PRIMARY risk
flagged in the xrd6 session. The full pre-push `Pkg.test()` gate (the safety net per Rule 14)
failed it: GATE B's `rehash!` wall disjunction (`:179`) still listed only the OLD
`@"_j_const#1"` memcpy wall, but u2kk advanced `rehash!` to the ptr-null ICmp wall
(`Bennett-bjdg / U80`). Fixed by adding `null`/`U80` disjuncts (a legitimate expectation
update reflecting the genuine advance, NOT a weakening); lf14 27/27, full suite 689893/1-broken-set
green. **Lesson: any change to a callee's `ptr_cells` extraction MUST re-run `test_lf14` (and
the `ptr_cells`-real-fn set) — it pins the per-callee landing wall.**

**Adversarial verifier (independent, opus): APPROVE-WITH-NITS.** Could not construct an
in-repo miscompile — the change is byte-identical to the proven plain-store path for the
4 real K=1 memcpys. **Residual cross-repo risk (item C, deferred to BVM):** the BVM
history tape MUST log+reverse a store into a caller-supplied pointer-PARAMETER cell with
BY-REFERENCE semantics — this is the IDENTICAL contract the already-accepted `setindex!`
field stores depend on, so u2kk surfaces a pre-existing whole-fdict-path dependency, not
a new one. Recorded on `bennettvm-416r.11`. Non-blocking nits (shared with alloca/arena
branches): byval/unsized-param undiscriminated acceptance; K>1 mixed-`elem_width`
chunk-stacking (eln6 byte-vs-cell class); no dst-cell bounds check (G8 checks only the
src global). Filed as `Bennett-h7r2`.

**Next.** `rehash!` now lands on `ConstantPointerNull operand: ptr null` in an
`ICmpInst` (a Dict-field null check) via `_operand(::LLVM.PointerNull)` at
`helpers.jl:198`. `Bennett-bjdg` (CLOSED) only fixed the error MESSAGE, not the
lowering — this is a fresh wall: extend the `ptr_cells` null→zero-cell handling (already
done for ret/insertvalue per `Bennett-beaw`) to the ICmp scalar-operand path. Filed as
`Bennett-8g7m`. Beads: `Bennett-u2kk` CLOSED; `Bennett-8g7m` (next wall) + `Bennett-h7r2`
(hardening) filed; cross-repo contract recorded on `bennettvm-416r.11`.

## Session log — 2026-06-29 — Bennett-xrd6: extract sret-convention consumed call to a registered callee (closed-world fdict CW-D path)

**Context.** Orchestrated "next most consequential bead" session targeting the P0
closed-world fdict critical path (cross-repo BennettVM EPIC `bennettvm-416r`, child
`416r.11` CW-D1, → fdict e2e `bennettvm-7xa`). Ground-truth recon confirmed CW-D1's
two completed sub-chunks: D1a (typed-callgraph walker, `src/extract/callgraph.jl`,
`transitive_callees`) and D1b (multi-IR producer, `src/extract/julia_set.jl`,
`extract_parsed_ir_set_from_julia`) are DONE and tested. The real blocker was the
per-callee extraction walls. Reproduced: `extract_parsed_ir_set_from_julia(fd,
Tuple{Int8,Int8}; ptr_cells=true)` with `fd(k,v)=(d=Dict{Int8,Int8}();d[k]=v;d[k])`
returned 0 bodies — everything walled.

**The wall (root cause).** Extracting the `setindex!(Dict{Int8,Int8},Int8,Int8)` callee
hit `ir_extract.jl: VoidType reached _type_width` (Bennett-dq8l / U81). The culprit:
`setindex!`'s body contains `call swiftcc void @"j_ht_keyindex2_shorthash!_NNN"(... sret({i64,i8}) %sret_box ...)` — the callee returns a `{i64,i8}` struct (index + shorthash)
via the sret convention, so the LLVM call site is typed `void`. The registered-callee
path in `_convert_instruction` (`src/extract/instructions.jl` ~2543–2557) called
`ret_w = _iwidth(inst)` on that void call → crash.

**Gotcha — isolation vs registered (Rule 7).** Extracting `setindex!` in isolation
succeeds (12 blocks) because `_lookup_callee` misses and the ptr_cells C-call arm
handles it gracefully. The wall surfaces ONLY once the producer calls
`register_callee!` for transitive callees, routing the call through the registered
arm. A second, interlocked wall lurked behind it: `rehash!` returns a `ptr`, so
clearing the void wall alone would have immediately hit `_type_width(PointerType)`.

**Process (CLAUDE.md Rule 2, 3+1 — core change to `instructions.jl`).** Raised bead
`Bennett-xrd6` (P1). Two independent read-only proposer subagents (opus). Proposer A:
model the `{i64,i8}` as already-in-memory via the `%sret_box` alloca — callee writes
it, existing read-back loads resolve it; IRCall carries a void/ptr→64 sentinel; no
`ir_types.jl` change, no scatter; also identified the `rehash!` ptr second wall.
Proposer B: add `ret_elem_widths` to IRCall + synthesise scatter via IRExtractValue +
IRStore; missed the `rehash!` wall. Orchestrator adjudicated Proposer A: more
complete (clears both walls), simpler, matches the existing BVM D5 sret ABI (smaller
cross-repo contract), and Rule 12 reuse.

**Done (`src/extract/instructions.jl`, +107/−22).** (1) New shared helper
`_cell_call_args(inst, ops, n_ops, names)` — the closed-world cell-ABI carry loop;
skips the swiftcc pgcstack synthetic via its call-site `swiftself` attribute (NOT by
name — Rule 5), carries integers at width and ptrs as 64-bit cells, fails loud
otherwise. (2) Registered-callee path gained a `ptr_cells` branch: fail-loud sret-
pointee validation (reusing `_sret_struct_fields` for dv1z packed/non-integer/bad-
width rejects), `_cell_call_args`, and a void/ptr→64 `ret_width` sentinel (Int→width,
ArrayType→`_type_width` NTuple, else fail-loud); the Function callee is kept. (3) The
`ptr_cells=false` integer-only gate-inlining path is untouched (gate-count baselines
are pinned to it). No `ir_types.jl` change; `_type_width` untouched.

**Test (`test/test_xrd6_sret_consumed_call.jl`, RED-first).** 16 structural-invariant
tests + 5 fail-loud `.ll` fixtures; registered at `runtests.jl:241`.

**Verified (orchestrator re-ran independently — not trusting the implementer).** `test_xrd6` 16/16 (`--check-bounds=yes`) / 21/21 (default); gate-count regression 39/39
IDENTICAL (circuit path byte-identical); `test_lf14` 27/27 with no expectation edits
(pre-written anticipating this wall move); `test_59zi` 545/545; `test_nd45` 71/71 +
`test_beaw` 17/17 (the riskiest shared-path check: the C-call arm now passes
`_cell_call_args(ptr_cells=true)`, whose only delta vs baseline is
ConstantPointerNull→zero-cell + the swiftself skip, which is a no-op for C callees).
**Producer advanced: 0 → 2 bodies** — `setindex!` and `ht_keyindex2_shorthash!` now
extract (confirmed via the `:skip` run showing both as in-set keys).

**Adversarial verifier (independent, opus): APPROVE-WITH-NITS.** Could not construct
an in-repo miscompile (Bennett.jl is a pure producer here; nothing executes a
`ptr_cells` IRCall in-repo). Nit: sret validation fires at attr index 1 (param 0) — a
Julia-swiftcc-specific assumption; a non-param-0 sret would skip validation (Julia
never emits that). **Residual cross-repo risk deferred to BVM D1c (explicit, not
hidden):** the consumed-sret IRCall is a thin implicit contract —
`[sret_box, Dict, key]` / ret=64 with no sret flag; BennettVM must infer args[1] is
the sret destination, reconcile caller-cell(64) vs callee-deref(512) Dict width, and
store the 72-bit `{i64,i8}` into the box so the `i64@0`/`i8@8` read-backs resolve. A
positional mis-bind there = silent miscompile; unproven until D1c asserts it.

**Closed `Bennett-xrd6`.** Filed **`Bennett-u2kk`** (P2): the producer now advances to
`rehash!`'s body wall —
`call void @llvm.memcpy.p0.p0.i64(ptr %"h::Dict.idxfloor_ptr", ptr @"_j_const#1", i64 8, ...)`
— a global-source memcpy into a Dict struct-field (`idxfloor` init), triggering
"memcpy dst operand is not alloca-backed" in `_handle_memcpy_arm`. vbv9-class. Note:
under `--check-bounds=yes` a different pre-existing wall surfaces first inside
`setindex!` — the iwo9 ptrtoint-on-`%memory_data` bounds artifact (Bennett-kvdv),
orthogonal. Cross-repo BVM D1c sret-binding contract risk also recorded on
`bennettvm-416r.11`.

## Session log — 2026-06-26 — Bennett-igr3 (cross-repo: landed in BennettVM, not Bennett.jl)

**Closed `Bennett-igr3`** (P1) — the BVM-side consumer of qmv7's `julia.gc_loaded`
emission. The work is in **BennettVM** (commit `3448897`, master), NOT Bennett.jl:
a new IRCall-arm in BennettVM `src/ir/ingest_body.jl` (after `_HEAP_DISPATCH`, before
the Float32/SoftCall path) maps the qmv7-emitted
`IRCall(Symbol("julia.gc_loaded"), [mem, data])` to the pointer-identity create
`Define(dest, data, :add, 0)` — the laundered data ptr IS the cell-addressed Memory
virtual base (the base qmv7's IRVarGEP/IRLoad/IRStore re-root onto); `mem` (args[1])
is structurally unread. BVM `test/test_igr3_gc_loaded_ingest.jl` (8 tests); full BVM
suite **6897/6897**; hostile reviewer APPROVE_WITH_NITS (operand order `[mem,data]` +
reversibility + dispatch ordering confirmed correct; nit applied: `_lower_ptr_operand`).
Full detail in `BennettVM/WORKLOG.md`. **Bennett.jl extraction is UNCHANGED** (qmv7
already round-trips IRVarGEP/IRLoad/IRStore; this was BVM-only per the bead). **Gotcha:**
Bennett.jl emits the UN-canonicalised `Symbol("julia.gc_loaded")` via the generic call
path, NOT a canonical `:gc_loaded` (contrast `:gc_alloc_obj`, canonicalised at
`instructions.jl:2636`). Pairs with `Bennett-jfw6` (closed below) toward the full fdict
e2e (`bennettvm-7xa`); next CW-D item is `Bennett-eln6` (i8 GEP byte-offset vs cell-index).

## Session log — 2026-06-26 — Bennett-jfw6: recon, verify, regression-lock, re-close (single-loop Case A); file ckkh; next = Dict Case B alloc wall

**Context.** Orchestrated "next most consequential bead" session. Picked `Bennett-jfw6`
(the `mem=:vm` Case A Vector/GenericMemory recogniser) — the declared LINCHPIN of the
opcode-coverage push (unblocks BennettVM Case A `xkl` + Case B route-b `tu9`).

**Surprise #1 — the bead text was stale.** jfw6's description (2026-06-04) says the
recogniser "is NOT yet implemented" and pins the reject at `module_walk.jl:198`. Both
wrong now: the 921-LOC five-file recogniser (`vector_vm{,_walk,_cfg,_term,_emit}.jl`)
DID land 2026-06-04, and `module_walk.jl:302-344` already dispatches `mem=:vm` →
`_dict_vm_extract` (Case B) / `_vec_vm_extract` (Case A) with a fail-loud reject at
:337. Line 198 is now the param-naming loop. (Rule 5 / Rule 10: bead line refs drift —
verify against the file, never trust the bead's line number.) The WORKLOG.md index even
records jfw6 as CLOSED 2026-06-04, yet the dolt store had it **OPEN (P1)** today —
likely a beads-sync/export artifact (cf. `reference_beads_sync_models` memory-drop +
status hazards). Re-closed below.

**Surprise #2 — recon overstated "complete".** A recon subagent ran ONE Vector shape,
got a 29-block ParsedIR, and reported "Case A extraction is COMPLETE and working." Rule-10
re-verification (my own `julia --project` runs) showed the recogniser handles ONLY a
**single `for i in 1:n` loop over one dynamic Vector**. Verified matrix:
  - **SUCCESS** — single for-loop: fused write+reduce (S1) OR write-loop + scalar read
    (S2) → multi-block ParsedIR with dynamic `IRAlloca(n_elems::SSAOperand)` +
    `IRVarGEP` + `IRStore`/`IRLoad`.
  - **FAIL-LOUD** (correct partition, NO miscompile): two for-loops "fill then sum" →
    `"two loop-bound icmp(1,n) tests — ambiguous loop preheader"`; `while` loop →
    `"not a for i in 1:n"`; ≥2 dynamic Vectors → `"SINGLE dynamic Vector"`; struct
    element → `"no element store/load"`.
The single-loop constraint is anchored in `_vec_vm_prelude_exit` (`vector_vm_cfg.jl:32`):
it locates THE one block holding the `icmp(1, n)` UnitRange-start test as the
prelude→user-CFG boundary; two for-loops emit two such icmps → ambiguous.

**Done.** Added `test/test_jfw6_vec_vm_extract.jl` (23 `@test`, green under
`--check-bounds=yes`): locks the two SUCCESS shapes (STRUCTURAL invariants only — Rule 5,
no block/gate counts: `>=2` blocks, exactly one `IRAlloca` with `n_elems isa SSAOperand`,
`>=1` IRVarGEP/IRStore) + the four fail-loud rejects (`@test_throws` + `occursin` on
stable message fragments). **First local coverage** for the recogniser (was zero — a
921-LOC silent-miscompile-risk pass with no Bennett.jl test; BennettVM
`test_vec_vm_roundtrip.jl` was the only proof). Registered at `runtests.jl:480`.

**Re-closed `Bennett-jfw6`** at its proven single-loop scope (matches the BennettVM
round-trip proof). Filed **`Bennett-ckkh`** (P2): expand to two-loop write-then-read — a
CORE 3+1 in `_vec_vm_prelude_exit` (re-derive the prelude exit as the alloc-prelude
`icmp(1,n)`; let the 2nd loop's preheader/φ pass through as user CFG). **NB:** its FULL
correctness can only be proven cross-repo via BennettVM's round-trip (BennettVM is not
loadable from Bennett.jl), so ckkh is a cross-repo effort, not a local quick win.

**Next (chosen): Dict Case B constructor-alloc wall.** `mem=:vm` on
`fd(k,v)=(d=Dict{Int8,Int8}(); d[k]=v; d[k])` fails LOUD at `dict_vm.jl:172`: the
`j_setindex!_NNN` callee survives (so `_dict_vm_is_recognised` routes to Case B) but the
`Dict{Int8,Int8}()` constructor's heap alloc does NOT surface as `ijl_gc_small_alloc` at
`optimize=false` on Julia 1.12.5 — the recogniser finds a mutation callee with no owning
allocation and rejects. On the fdict critical path (BennettVM `7xa`).

**Housekeeping (2026-06-26).** Re-synced beads in both repos at session start (git 0/0;
`bd import` 582 / 216). Re-confirmed + handled the BennettVM auto-export memory-drop
hazard (`issues.jsonl` 220→216 dropping 4 `_type:memory` records; restored to HEAD) and
the Bennett.jl `repo_state.json` machine-path flip — see `reference_beads_sync_models`.
