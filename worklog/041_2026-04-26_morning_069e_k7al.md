# Bennett.jl Work Log

## Session log — 2026-04-26 (late-morning) — 8kno + zy4u closes (LLVM.initializer narrowing + outer @testset)

**Shipped:** see `git log` `2b8a18a..4942342` (5 commits). Two beads closed, one new P3 follow-up filed.

| Bead | What |
|---|---|
| **Bennett-8kno** P3 / U95 (BUG) | Extended Bennett-uinn's narrowing pattern to `_extract_const_globals`'s catch around `LLVM.initializer(g)`. uinn (chunk 040) had added `e isa InterruptException && rethrow()` to all bare catches in `ir_extract.jl`, but this site's specific concern was that OOM / StackOverflowError / MethodError were still being swallowed alongside the legitimate "Unknown value kind" / "LLVMGlobalAlias" errors that justify the catch. Now the catch matches LLVM.jl's specific error strings via `e isa ErrorException && (occursin("Unknown value kind", msg) || occursin("LLVMGlobalAlias", msg))` — same `benign ? nothing : rethrow()` pattern as the precedent at line 989 (`_convert_instruction` outer catch). New `test_8kno_extract_const_globals_narrowing.jl` (8 asserts) static-inspects the function body for the InterruptException + benign-substrings + conditional-rethrow pattern. |
| **Bennett-zy4u** P3 / U104 | Wrapped `test/runtests.jl` in `@testset "Bennett" begin ... end`. Pkg.test now produces a single named aggregate Test Summary at the end instead of N flat per-include summaries. Body is intentionally NOT reindented (Julia parses `begin ... end` blocks regardless of interior indentation; reindenting ~80 `include(...)` lines would have dominated the diff with whitespace + broken `git blame`). Conditional gates (BENNETT_T5_TESTS, BENNETT_RESEARCH_TESTS) are inside the wrapper. The bead also asked for fast-tests-first / slow-tests-last reordering; that needs per-file empirical timing — filed as **Bennett-qjet (P3, NEW)**. |

**Why:** continuation of catalogue grind. 8kno was a one-line follow-on to uinn's narrowing pattern; trivial. zy4u was a structural improvement to test infrastructure with real ergonomic value (one named aggregate instead of dozens of per-include summaries) — it's been bumped multiple sessions because each prior session had a more attractive "more serious" pick.

**Gotchas / Lessons:**

- **Julia parses `@testset "Bennett" begin ... end` regardless of interior indentation.** First instinct was to mass-reindent ~80 `include(...)` lines for visual nesting; rejected on diff-hygiene grounds. Verified by Pkg.test that the unindented body works fine; testsets nest correctly. *Lesson*: when wrapping a large block in a Julia macro that uses `begin ... end`, keep interior indentation untouched — same outcome, vastly cleaner diff.

- **The catch-narrowing pattern from `_convert_instruction`'s outer catch (line 989) is reusable across `ir_extract.jl`.** That site had: `e isa InterruptException && rethrow(); benign-check; benign ? nothing : rethrow()`. 8kno transcribed this pattern to `_extract_const_globals`. Future agents narrowing other defensive catches in this file: copy that template, swap in the file-specific benign-substring check.

- **Verified `bd close` works fine with `--reason="..."` containing backticks if you use a shell HEREDOC** but NOT in a single-line `--reason="...\`code\`..."` form. The prior session bit me with `LLVM.Context() do _ctx` — bash interpreted the backticks as command substitution. Workaround in this session: rephrase to avoid backtick code spans in close notes ("'@testset Bennett begin ... end'" instead).

- **Followup-beads-from-partial-closure pattern works well.** zy4u's bead asked for two distinct things (wrap + reorder); shipped (wrap), filed Bennett-qjet for (reorder) with the rationale that it needs empirical timing data. Total session work: full close on zy4u + new bead with self-contained context. Future-agent reading qjet has the unblocking criteria + suggested approach. This pattern (close-the-easy-half, file-the-hard-half) is right for any review bead with a clearly-bisectable scope.

- **Pkg.test's outer-test-summary structure is now visible to anyone running it.** Before zy4u, the only way to read the success state was the literal "Testing Bennett tests passed" line emitted by Pkg.test's wrapper. Now there's also a `Test Summary: Bennett | Pass Total Time` aggregate inside the test output, which is what most downstream tooling (CI dashboards, etc.) parses. Useful for the 0-CI Bennett.jl world.

**Rejected alternatives:**

- **Reindenting all ~80 `include(...)` lines for visual nesting.** See above — rejected on diff-hygiene grounds. The wrapped form `@testset "Bennett" begin\ninclude(...)\nend` is parsed correctly without indentation.

- **Reordering tests fast→slow as part of zy4u.** Without per-file timing data, the reorder would be guesswork. Filed Bennett-qjet to do it properly with `@elapsed` instrumentation.

- **Bennett-zmw3 (shift bug) for the more-serious slot.** 7th session deferred. It needs the 3+1 protocol per CLAUDE.md §2 and a dedicated session with parallel proposer agents. Worth one whole session of focused work.

**Next agent starts here:**

1. **Branch state at session-end**: `4942342` on main, pushed. Worklog top is **this** entry; chunk 041 is now ~210 lines (was ~150 pre-this-session). Approaching the 280 cap — next session may want to start `worklog/042_*.md`.

2. **Catalogue progress this session (2 closes + 1 new bead)**: ~141 → ~140 ready remaining (8kno, zy4u closed; qjet new). Cumulative for the day's grind = 14 closes (6t8s, ej4n, 348q, tfo8, 2jny, kmuj, uzic, uinn, 069e, k7al, pksz, zyjn, 8kno, zy4u). Test count growth: +437 new assertions across 10 new test files.

3. **Quick wins still on the menu** (each ~30-90 min, no 3+1 needed):
   - **Bennett-vpch** U45 — 190+ `error()` → typed exceptions (substantial; needs taxonomy first).
   - **Bennett-59jj** U47 — type instability in hot paths.
   - **Bennett-zpj7** U160 — pebbling/eager file rename.
   - **Bennett-5kio** U109 — sizehint! across 142+69 push! sites.
   - **Bennett-d1ee** U141 — missing WHY comments at load-bearing invariants.
   - **Bennett-f6qa** U97 — lower.jl error sites lack cc0.6 prefix; pebbling wording inconsistent.
   - **Bennett-3of2** U112 (BUG) — `_cond_negate_inplace!` leaks per-bit carry ancillae. Real correctness bug; touches lower.jl so 3+1 is technically required, but the fix scope is narrow (route ancillae through Bennett or uncompute at op end).
   - **Bennett-qjet** P3 — zy4u-followup, empirical timing-based reorder (NEW from this session).

4. **3+1-protected real bugs still open** (unchanged): jepw, 25dm, 5qrn, zmw3, y986. zmw3 has been bumped 7 sessions running — needs a dedicated session.

5. **The catch-narrowing pattern is reusable.** uinn (chunk 040) → 8kno (this session) demonstrates the template: `e isa InterruptException && rethrow()` for any bare catch, plus a benign-substring check + `benign ? nothing : rethrow()` for any catch where there's a known LLVM.jl-specific error string. Worth applying to other modules' catches if a similar bead surfaces (none currently filed, but `find src -name '*.jl' | xargs grep -l "catch$\|catch;"` is the standing test).

---

## Session log — 2026-04-26 (mid-morning) — pksz + zyjn closes (controlled-wire invariant + deref-bytes distinct failures)

**Shipped:** see `git log` `cb92e57..37b738d` (4 commits). Two beads closed.

| Bead | What |
|---|---|
| **Bennett-pksz** P3 / U98 | Added a contiguous-wire-allocation invariant assert at the top of `controlled()` in `src/controlled.jl`. The `ReversibleCircuit` constructor partitions input/output/ancilla over `1:n_wires` but does NOT cross-check that the gate stream stays inside that range — so a malformed circuit with `gates = [NOTGate(n_wires + 1)]` would silently collide with the `ctrl_wire` we allocate at `n_wires + 1`. New `_gate_max_wire` helper + a guard that errors with attribution to the bead. New `test_pksz_controlled_contiguous_wires.jl` (13 asserts) covers happy path, malformed-rejection path, and the empty-gates edge case. |
| **Bennett-zyjn** P3 / U94 (BUG) | `_get_deref_bytes` in `src/ir_extract.jl` collapsed THREE distinct outcomes into the same silent return-0 value: (a) param not in `func.parameters` (caller-side miswiring), (b) defline missing the `(...)` parameter list (LLVM.jl format mismatch), (c) param has no `dereferenceable(N)` attribute (legitimate "no info"). Now (a) and (b) `error()` with bead attribution; only (c) still returns 0. Caller pattern (`deref > 0` at line 908) unchanged. New `test_zyjn_deref_bytes_distinct_failures.jl` (5 asserts) covers happy path on a real `Tuple{Int8,Int8}`-input function, caller-bug rejection (param from func A against func B), and the legitimate no-attr return-0 case. |

**Why:** continuation of catalogue grind. pksz was a focused defensive-assert addition; zyjn was a real bug-class fix where two latent caller-side-bug paths used to silently produce the same value as a common legitimate path, defeating any future debugger from telling them apart.

**Gotchas / Lessons:**

- **`ReversibleCircuit` only partitions wire indices over input/output/ancilla — it doesn't validate the gate stream.** Realised this writing pksz: `gates.jl:53-83` checks `union(in, out, anc) == 1:n_wires` AND no overlaps, but a malformed `NOTGate(99)` with valid in/out/anc=1:5 would pass. Could file a follow-up bead to extend the partition validator to also cross-check gates, but current shape is OK because `controlled()` is the one place where a gate-index assumption matters. *Lesson*: type-level invariants are local; cross-check the data each consumer needs at the point of use.

- **LLVM.jl Module/Type construction must use the `LLVM.Context() do _ctx ... end` block pattern.** First version of `test_zyjn` used `LLVM.Module("name"; ctx=...)` — newer LLVM.jl rejects the kwarg form with a `MethodError: no method matching LLVM.Module(::String; ctx::Context)`. The codebase's existing tests (`test_cc06_error_context.jl`, `test_lower_store_alloca.jl`, `test_memory_corpus.jl`) all use the implicit-context block pattern; matching that fixed it. *Pattern for future LLVM tests*: copy from `test_cc06_error_context.jl` lines 32-58 verbatim.

- **`bd close` interpreted backticks in the close message as bash command substitution.** Got two warnings (`/bin/bash: line 1: ctx: command not found` and a syntax error on `do`) from the shell trying to evaluate `LLVM.Context() do _ctx`. The bd close itself succeeded but those backtick-bracketed code spans were stripped from the saved message. *Pattern for future*: in HEREDOCs and `bd close --reason="..."`, escape backticks or rephrase code spans to avoid them. Used a different format in subsequent close notes.

- **The constructor-validation work from k7al (chunk 041 prior session) is paying compound dividends.** pksz found that the `ReversibleCircuit` partition validator runs on construction but doesn't check gates — so the pksz fix is essentially a *post-construction* invariant check applied at the start of `controlled()`. Future similar "this assumption is implicit" beads can reuse the same shape: assert at the consumer, not the constructor.

- **Constructing LLVM modules in tests is an option for unit-testing internal extraction helpers.** Until zyjn I assumed all ir_extract-internal tests had to go through `reversible_compile`. The `test_cc06_error_context.jl` precedent shows you can `LLVM.Function(mod, name, ftype)` directly to set up minimal test fixtures — useful for any helper that takes `LLVM.Function` / `LLVM.Argument` arguments.

**Rejected alternatives:**

- **Returning `Union{Nothing, Int}` from `_get_deref_bytes`.** Cleaner type-wise; would force the caller to switch from `deref > 0` to `deref !== nothing && deref > 0`. Skipped because the current `Int` return + 0-on-no-attr semantics work fine and the bead's actual concern is BUG-vs-EXPECTED distinction, not the return type. Kept the API stable.

- **Extending `ReversibleCircuit`'s partition validator to cross-check gates.** Tempting (would solve pksz at the constructor level instead of at `controlled()`), but: (a) the partition check runs on every construction including hot paths in `bennett()`/`controlled()`/strategy variants — adding O(gates) wire-bound checks would touch perf; (b) the gate-vs-partition cross-check is meaningfully different work from the partition coverage check (different invariant, different data). Filed conceptually as a possible follow-up; not a current bead.

- **Bennett-zmw3 (shift bug)** for the more-serious slot. Same call as the prior 5 sessions — needs 3+1 protocol + dedicated session.

**Next agent starts here:**

1. **Branch state at session-end**: `37b738d` on main, pushed. Worklog top is **this** entry; chunk 041 is now ~150 lines (was ~80 pre-this-session).

2. **Catalogue progress this session (2 closes)**: ~143 → ~141 ready remaining. Cumulative for the day's grind = 12 closes (6t8s, ej4n, 348q, tfo8, 2jny, kmuj, uzic, uinn, 069e, k7al, pksz, zyjn). Test count growth across the day: +429 new assertions across 9 new test files.

3. **Quick wins still on the menu** (each ~30-90 min, no 3+1 needed):
   - **Bennett-vpch** U45 — 190+ `error()` → typed exceptions (substantial; needs taxonomy first).
   - **Bennett-59jj** U47 — type instability in hot paths.
   - **Bennett-zy4u** U104 — runtests.jl outer @testset + reorder fast→slow.
   - **Bennett-zpj7** U160 — pebbling/eager file rename.
   - **Bennett-5kio** U109 — sizehint! across 142+69 push! sites.
   - **Bennett-d1ee** U141 — missing WHY comments at load-bearing invariants.
   - **Bennett-f6qa** U97 — lower.jl error sites lack cc0.6 prefix; pebbling wording inconsistent.
   - **Bennett-8kno** U95 — likely already addressed by Bennett-uinn (chunk 040): the `_extract_const_globals` bare-catch was one of the 11 sites narrowed in uinn. Worth a quick triage close-as-fixed-by.

4. **3+1-protected real bugs still open** (unchanged): jepw, 25dm, 5qrn, zmw3, y986. zmw3 has been bumped 6 sessions running.

5. **The `_get_deref_bytes` zyjn distinction pattern is reusable.** Other beads in the catalogue probably ask for similar "distinguish bug-paths from expected-paths in a function that swallows everything to `nothing`/0/[]". When the next "silent-failure ambiguity" bead comes up: list the failure modes, classify each as bug-path (→ `error()` with attribution) or expected-path (→ keep current sentinel), update the docstring, write a unit test per category. That's the playbook.

---

## Session log — 2026-04-26 (morning) — 069e + k7al closes (DP sentinels named + IR constructor validation)

**Shipped:** see `git log` `91c710b..9f76a8c` (4 commits). Two beads closed.

| Bead | What |
|---|---|
| **Bennett-069e** P3 / U143 | Replaced 6 sites of bare `typemax(Int)÷2` / `typemax(Int)÷4` magic in `src/pebbling.jl` with two named consts: `_PEBBLE_INF` (init sentinel for "no finite cost yet") and `_PEBBLE_FINITE_BOUND` (gate threshold below which a cell holds a real cost). Comment block at the top documents the two invariants: `3·_PEBBLE_FINITE_BOUND < typemax(Int)` (no Int overflow on `a+b+c`) and `_PEBBLE_INF >= _PEBBLE_FINITE_BOUND` (sentinel fails the finite-cost gate). New `test_069e_pebble_sentinels.jl` (19 asserts) pins arithmetic + Knill 1995 base cases F(1,s)=1, F(n,1)=∞, F(2,2)=3. |
| **Bennett-k7al** P3 / U99 | Added inner constructors with field validation to 11 IR struct types in `src/ir_types.jl`. New module-scoped const tables (`_IR_OPERAND_KINDS` / `_IR_BINOP_OPS` / `_IR_ICMP_PREDS` / `_IR_CAST_OPS`) hold the canonical symbol sets. Constructors validate: op/predicate/kind symbol membership for IROperand/IRBinOp/IRICmp/IRCast; `width >= 1` for arithmetic / cast / ret / load / store / alloca / call (no zero-bit ops); `width >= 0` for IRSelect / IRPhi (relaxed because Bennett-cc0 M2b uses width=0 as a pointer-typed sentinel — caught by the test_memory_corpus L7c/L7d tests on the first Pkg.test); IRCall arity match + non-empty arg widths; IRPhi non-empty incoming. New `test_k7al_ir_constructor_asserts.jl` (96 asserts). |

**Why:** continuation of catalogue grind. 069e was the cleanest contained doc-magic-elimination remaining. k7al was the substantive defensive fix: the bead's premise was real — typo'd op symbols would silently route through `lower.jl`'s elseif chain and crash with an unhelpful "Unknown binop: :foo" error far from the call site. Constructor-level validation surfaces the bug at the emitter.

**Gotchas / Lessons:**

- **Width=0 IS a real sentinel for pointer-typed SSAs.** First version of k7al asserted `width >= 1` universally. test_memory_corpus L7c (pointer-typed phi) and L7d (pointer-select) errored on the first Pkg.test because `ir_extract.jl:1268` and `:1283` deliberately emit `width=0` for pointer-typed selects/phis (Bennett-cc0 M2b — pointers don't materialise as wires; routing lives in `ptr_provenance`). Relaxed IRSelect / IRPhi to `width >= 0` with a comment documenting the sentinel. *Generalisable lesson*: whenever adding "field >= 1" asserts, grep for `width = 0`, `w = 0`, `width=0` first to find any pre-existing sentinels. The grep would have caught both M2b sites pre-emptively.

- **Background `julia ... 2>&1 | tail -N` truncates `Pkg.test` output to a stack-trace fragment.** First run of k7al's full suite with `tail -8` showed only a stack-trace tail (Pkg.Operations frames) — exit code 0 and no obvious failure markers. Re-ran with `tail -100` and the actual `IRSelect: width=0 must be >= 1 (dest=p)` error surfaced. **Pattern to use**: when capturing background `Pkg.test`, use `grep -E "tests passed|tests failed|Test Failed|Got exception|Error During|^ERROR"` rather than line-count tail; OR use a larger window (≥150 lines). The line-count tail risks landing entirely inside a stack-trace and giving you no signal.

- **Inner constructors REPLACE the auto-generated default.** Once you add a single `function ...; new(...) end` inside a struct, the auto-generated all-fields default disappears. So the inner constructor must accept exactly the same positional signature as the field list, in field order. Easy to get right but easy to forget — a dropped field name in the constructor signature is silent.

- **`@test_throws "substring"` matches by message substring.** Used heavily in test_k7al — every rejection path tests both type AND a unique substring of the error message ("op=:bogus", "width=0", "ret_width=0", "incoming is empty", etc.). A future agent who edits an error message in ir_types.jl will see the substring assert fire as a localised diagnostic; the constructor body still does what it should but the test surface tells you exactly which message moved. Same convention used by Bennett-0zsk (chunk 039).

- **The validation tables ARE the documentation.** `_IR_BINOP_OPS = (:add, :sub, :mul, :and, :or, :xor, :shl, :lshr, :ashr, :udiv, :sdiv, :urem, :srem)` replaces the previously-stale comment `# :add, :sub, :mul, :and, :or, :xor, :shl, :lshr, :ashr` (which omitted the four div/rem variants). The new const can't go stale — adding a new op requires updating it, OR the constructor will error and the test will fail. Future agents adding IR ops: append to the matching const tuple AND update test_k7al's per-op happy-path testset.

**Rejected alternatives:**

- **Cross-field consistency checks** like "IRBinOp.op1 and IRBinOp.op2 widths match `width`". The widths of operands aren't stored on `IROperand` (they live in `IRBinOp.width` for both); the cross-check would have to happen at lowering time. Skipped — out of scope for k7al.

- **`@assert` instead of `error()`** for the constructor body. CLAUDE.md §1 ("FAIL FAST, FAIL LOUD") prefers explicit `error(...)` with context over `@assert`, and `@assert` is stripped by `--check-bounds=no`. Used `error()` everywhere; matches the existing pattern in the codebase.

- **`Union{Int, Nothing}` for IRSelect.width / IRPhi.width** instead of the `0` pointer sentinel. Cleaner Julia type-wise but: (a) breaks every existing call site that reads `.width` arithmetically, (b) the M2b sentinel convention is well-established with comments at the emitter sites, (c) lower.jl's `_pick_alloca_strategy` and friends already check `width == 0` for pointers. Kept the sentinel; documented it.

- **Bennett-zmw3 (shift bug)** for the more-serious slot. Same call as the prior 4 sessions — it's 3+1 territory and needs a dedicated session with parallel proposer agents.

**Next agent starts here:**

1. **Branch state at session-end**: `9f76a8c` on main, pushed. Worklog top is **this** file (`worklog/041_*.md`, ~80 lines pre-this-line + this entry). Chunk 040 stayed at ~210 lines.

2. **Catalogue progress this session (2 closes)**: ~145 → ~143 ready remaining. Cumulative for the day's grind = 10 closes (6t8s, ej4n, 348q, tfo8, 2jny, kmuj, uzic, uinn, 069e, k7al). Test count growth: +411 new assertions across 7 new test files.

3. **Quick wins still on the menu** (each ~30-90 min, no 3+1 needed):
   - **Bennett-vpch** U45 — 190+ `error()` → typed exceptions (substantial; needs taxonomy first).
   - **Bennett-59jj** U47 — type instability in hot paths.
   - **Bennett-zy4u** U104 — runtests.jl outer @testset + reorder fast→slow.
   - **Bennett-zpj7** U160 — pebbling/eager file rename.
   - **Bennett-5kio** U109 — sizehint! across 142+69 push! sites.
   - **Bennett-pksz** U98 — controlled.jl missing contiguous-wire invariant assert.
   - **Bennett-d1ee** U141 — missing WHY comments at load-bearing invariants.
   - **Bennett-f6qa** U97 — lower.jl error sites lack cc0.6 prefix; pebbling wording inconsistent.

4. **3+1-protected real bugs still open** (unchanged): jepw, 25dm, 5qrn, zmw3, y986. zmw3 has been bumped 5 times now — worth a dedicated session with two parallel proposer agents.

5. **The k7al validation pattern is reusable.** Other beads in the catalogue ask for similar constructor-level validation (e.g. ReversibleCircuit's wire-partition invariant is already inline-asserted at `gates.jl:53-83` — that's the existing model). Future "the constructor accepts garbage" beads in this codebase: same playbook — module const for the canonical set, inner constructor with `error(...)`, `@test_throws "substring"` per rejection path.

6. **Output-capture pattern for background `Pkg.test`** (lesson above): prefer `grep -E "tests passed|tests failed|Test Failed|Got exception|Error During|^ERROR"` over `tail -N`. Codify this in `feedback_pkg_test_capture.md` if it bites again.
